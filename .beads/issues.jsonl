{"_type":"issue","id":"apw-d7u","title":"The robot damage, health and speed levers may not reach the game","description":"Reported: 'dont think the robot damage/health/speed things are working, did not\nsee any difference even putting them at 30%'.\n\n## What is proven\n\nThe launcher half is correct. RenderServerCfg with all three at 30 writes:\n\n  sm_redbots_manager_blu_damage_scale 0.30\n  sm_redbots_manager_blu_health_scale 0.30\n  sm_redbots_manager_blu_speed_scale 0.30\n\nThe dialog reads the boxes back into the settings, the convar names match the\nones BluAssist_Init creates, the bounds accept 0.30, and the pin in\ndeploy/env/versions.env is v2.21.0, whose tag does contain blu_assist.sp. So\nnothing in this repo explains it.\n\nDamage reaches the game at least sometimes: the testbed arm at 0.5 cleared a\nwave once in eight attempts against zero in twenty four for the unchanged build,\nand moved robots-killed well outside the band. That was set over rcon, not\nthrough server.cfg, which is the difference this bead has to close.\n\n## The two candidates\n\n1. The value never arrives. server.cfg is executed once at server start; if the\n   plugin registers its convars after that exec, the line sets nothing and the\n   default of 1.0 stands. Nobody has read the convar back off a running server.\n\n2. Health and speed do not stick. BluAssist_OnRobotSpawn writes m_iMaxHealth and\n   the move speed bonus attribute on player_spawn. An MvM popfile applies its own\n   health and attributes to a robot, and if it does so after that event, ours is\n   overwritten and the robot spawns exactly as the mission wrote it. Damage would\n   be unaffected, since BluAssist_TakeDamage scales the hit rather than the robot.\n\nThe two predict different things: candidate 1 breaks all three, candidate 2\nbreaks health and speed and leaves damage working.\n\n## How to tell, cheapest first\n\nrcon the running server for sm_redbots_manager_blu_damage_scale. 1.0 is\ncandidate 1 and the fix is in this repo. 0.30 rules it out and the fault is in\nthe mod.\n\nThe health arm of mvm-bha is running now against the Bavarian Botbash band. If\n0.5 health shows nothing while 0.5 damage bent the mission, that is candidate 2\nwith a measurement behind it.","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T14:06:32Z","created_by":"m-this","updated_at":"2026-08-27T19:47:21Z","started_at":"2026-08-27T14:48:30Z","closed_at":"2026-08-27T19:47:21Z","close_reason":"Fixed in defenderbots v2.21.2 and verified.\n\nTwo faults, both mod side. BluAssist_OnRobotSpawn ran inside the player_spawn\nframe, where the popfile is still building the robot and overwrote it; it now\ndefers a frame. And a raw m_iMaxHealth write does not stick because TF2\nrecomputes the maximum from attributes, so health goes through 'max health\nadditive bonus' as a delta. Speed was also replacing the template's move speed\nbonus instead of multiplying it.\n\nMeasured against the Bavarian Botbash band: robots killed 62.5 before, 108\nafter, band 52-64, no overlap, and three clears in eight against zero in\ntwenty four.\n\nThe launcher was never at fault. RenderServerCfg writes the right lines and a\nreal server's console log shows its sibling convars applying without complaint.","comments":[{"id":"01a0438c-1207-791a-962e-e63b3dd5f487","issue_id":"apw-d7u","author":"m-this","text":"The health arm points at candidate 2, on 7 of 8 attempts.\n\nHealth 0.5 on Rottenburg advanced1 wave 1, against the same band:\n\n                  band (24)        health 0.5 (7)\n  cleared         0                1\n  held            160s q154-192    median 186.2\n  robots killed   58  q52-64       median 61\n  defenders died  24  q22-31       median 32\n\nRobots killed is the number that matters. Robots at half health should die in\nhalf the shots, so that median should move a long way up. It sits at 61, inside\nthe band's own quartiles. Damage at 0.5 moved the same number to 75, outside it.\n\nBy the rule in docs/testbed-metrics.md an arm whose median falls inside the\nband's quartiles has shown nothing, and this one has shown nothing on the one\nmeasure it could not fail to move if it worked. Defenders died is slightly worse\nthan the band, which is the wrong direction for a lever meant to help.\n\nSo damage reaches the robots and health looks like it does not, which is what\ncandidate 2 predicts: BluAssist_OnRobotSpawn writes m_iMaxHealth on player_spawn\nand an MvM popfile writes the robot's real health after it.\n\nNot final: seven attempts, one clear, and the eighth is still running. The rcon\nread is still the first thing to do, since it separates the two candidates\noutright.","created_at":"2026-08-27T14:07:28Z"},{"id":"01a043b3-3111-727d-828d-bd0c8506060e","issue_id":"apw-d7u","author":"m-this","text":"Candidate 2 holds on reading the code, and the mod half is fixed.\n\nsource/redbots3/events.sp calls BluAssist_OnRobotSpawn straight out of the\nplayer_spawn handler. The popfile is still building the robot at that point: it\ngives the template's items, fires post_inventory_application, then writes the\nmission's health and attributes, all inside the same frame. So m_iMaxHealth and\n\"move speed bonus\" were written and then overwritten before the robot took its\nfirst step. Damage escaped because BluAssist_TakeDamage scales the hit, not the\nrobot, which is exactly the split the measurement showed: damage 0.5 moved\nrobots killed to 75, health 0.5 left it at 61 inside the band.\n\nTwo things were wrong, not one:\n\n1. Timing. The bend now runs on RequestFrame from player_spawn, after the\n   popfile has finished. The frame callback re-checks the client is in game,\n   alive, BLU and a bot, since a robot can die or leave in between.\n\n2. Health would not have stuck even with the right timing. TF2 recomputes\n   maximum health from the attributes, so a raw m_iMaxHealth write gets the\n   popfile's number back. Health now goes through \"max health additive bonus\"\n   as a delta added to whatever the popfile set, so a giant stays a giant,\n   scaled, and a common scales the same way. m_iMaxHealth and current health\n   are still written so the robot spawns at the new maximum rather than hurt.\n\nSpeed also composes now: it multiplies the template's own move speed bonus\ninstead of replacing it, or a fast robot and a normal one came out at the same\nspeed.\n\nFiles: source/redbots3/blu_assist.sp, source/redbots3/events.sp. Compiles clean.\n\nNot closing this. It cannot be closed without a testbed run, which this session\nmay not do: a measurement run is in flight and rebuilding would void it.\n\nWhat would prove it: the same arm the health measurement used, health 0.5 on\nRottenburg advanced1 wave 1 against the Bavarian Botbash band, eight attempts.\nRobots killed has to move well above the band's q52-64, near the 75 that damage\n0.5 reached. Anything inside the quartiles means the bend still is not landing.\nThe rcon read of sm_redbots_manager_blu_damage_scale on a running server is\nstill worth doing, since candidate 1 would break all three levers on top of\nthis and is fixed in the other repo, not here.\n","created_at":"2026-08-27T14:50:12Z"},{"id":"01a043b8-9ff6-7891-9ee9-48049d5ff700","issue_id":"apw-d7u","author":"m-this","text":"Speed 0.5 measured too, and it also shows nothing.\n\n                  band (24)      damage 0.5   health 0.5   speed 0.5\n  attempts        24             8            8            6 of 8\n  cleared         0              1            1            0\n  robots killed   58 q52-64      75           62.5         57.5\n  held            160s q154-192  -            190.8        167.6\n\nTwo of the three sit inside the band's quartiles on every measure. Damage is the\nonly one outside it. That is the split the overwrite theory predicts, since\ndamage scales the hit and the other two write the robot at spawn.\n\nTwo of the eight speed attempts produced no wave result and one crashed, so that\narm is six. It does not change the reading: nothing about it is near the band's\nedge.\n\nFix is in, mod side, v2.21.2. Health 0.5 is running again against the same band\nto say whether it lands.","created_at":"2026-08-27T14:56:08Z"},{"id":"01a043bb-456f-73d6-a2f0-3dcb8cf04432","issue_id":"apw-d7u","author":"m-this","text":"Mod fix merged, defenderbots v2.21.2. Two faults, not one.\n\nTiming: BluAssist_OnRobotSpawn ran inside the player_spawn frame, and the\npopfile finishes building the robot in that same frame, giving the template's\nitems, firing post_inventory_application, then writing the mission's health and\nattributes. Everything written from the event was overwritten. It now defers a\nframe and re-validates in game, alive, BLU, fake client.\n\nUnderneath it: a raw m_iMaxHealth write does not stick anyway, since TF2\nrecomputes the maximum from attributes. Health now also goes through\n'max health additive bonus' as a delta added to what the popfile set, so a giant\nstays a giant scaled.\n\nThird thing found on the way: speed replaced the template's own move speed bonus\ninstead of multiplying it, so a fast robot and a normal one came out walking at\nthe same speed. Both bends now compose with the mission.\n\nHealth 0.5 is running again against the same band. Robots killed has to move\nwell clear of q52-64, near the 75 damage reached. Inside the quartiles means it\nstill is not landing.\n\nOpen regardless of that run: the rcon read of\nsm_redbots_manager_blu_damage_scale on a live server. If it reads 1.0 then\nserver.cfg never reached the plugin and there is a second fault in the launcher\non top of this one.","created_at":"2026-08-27T14:59:02Z"},{"id":"01a043d5-b9a7-708a-a9df-4a7c0fb968a5","issue_id":"apw-d7u","author":"m-this","text":"The fix lands. Health 0.5 re-run on v2.21.2, 5 of 8 attempts:\n\n                  band (24)      health 0.5 before   health 0.5 after\n  robots killed   58 q52-64      62.5                108\n  held            160s q154-192  190.8               228.0\n  defenders died  24 q22-31      33.0                25.0\n  cleared         0 of 24        1 of 8              1 of 5\n\nRobots killed goes from inside the band's quartiles to 108, with every attempt\nbetween 102 and 108 and no overlap with the band at all. Before the fix that\nnumber would not move; now it doubles. The lever reaches the robots.\n\nDefenders died also comes back down to the band, which the broken version had\nmade worse.\n\nSo the write-overwritten diagnosis was right and the frame deferral plus the\nmax health additive bonus route fixes it. Speed shares that path and should\nfollow, though it has not been re-measured.\n\nStill not closable. Two things left:\n\n- the rcon read of sm_redbots_manager_blu_damage_scale on a live server. All of\n  the above was set over rcon in the test-bed, so it says the mod works, not\n  that server.cfg reaches it. If that reads 1.0 there is a second fault in the\n  launcher.\n- one clear in five is not a mission bent into winnable. Robots killed proves\n  the lever works; whether 0.5 is the right number for players is a separate\n  question.","created_at":"2026-08-27T15:27:55Z"},{"id":"01a044c3-35ee-7b15-b35c-78adee0169ee","issue_id":"apw-d7u","author":"m-this","text":"Candidate 1 is refuted, on evidence rather than by a live rcon read.\n\nk-kaneta's bundle has a real launcher-started server: its server.cfg carries nine\nsm_redbots_manager_* lines and the console log has no 'Unknown command' for any\nof them. Those convars are registered by the same plugin, in the same file, at\nthe same exec. So server.cfg does reach the plugin, and a line added beside them\nreaches it too.\n\nTheir build predates the balancing convars, so the three scale lines are not in\nthat file. What it proves is the delivery path, which is what candidate 1\nquestioned.\n\nThat leaves candidate 2 as the whole of it, and candidate 2 is fixed and\nmeasured: robots killed went from 62.5, inside the band's q52-64, to 108 across\neight attempts with no overlap, and three of eight waves cleared against zero in\ntwenty four.\n\nClosing. Damage worked all along, health and speed were overwritten at spawn and\nnow are not.\n\nLeft as a note rather than a bead: speed has not been re-measured since the fix.\nIt shares the write path with health and should follow, but should is not\nmeasured. Worth an arm the next time the test-bed is idle.","created_at":"2026-08-27T19:47:19Z"},{"id":"01a044e4-5143-7e13-8bb4-e284bf07b5ee","issue_id":"apw-d7u","author":"m-this","text":"Speed was never re-measured after the fix and damage was only ever inferred from\na wave outcome, so both are now instrumented rather than argued, in defenderbots\nv2.22.1.\n\nBluAssistSay writes one line per 25 robots saying what that robot's health and\nmove speed bonus actually are after the bend, against what was wanted, plus\nm_flMaxspeed. A second sample does the same for damage: what the hit was, what\nit became, and the scale asked for.\n\nA wave outcome cannot tell a lever that is not applying from one that is\napplying and not enough, and that ambiguity is what made this report hard to\nplace in the first place.\n\nRun queued: all three at 0.5 on Rottenburg advanced1 wave 1, behind the two A/Bs\nalready in the queue. The bead stays closed unless the lines say a lever is not\nlanding, in which case it reopens with the numbers.","created_at":"2026-08-27T20:23:29Z"}],"dependency_count":0,"dependent_count":0,"comment_count":7}
{"_type":"issue","id":"apw-e0g","title":"The buff window opens with nothing in it","description":"Reported: 'dont show the buff window on screen if there are no buff available\nfor this loadout'.\n\nWeaponBuffs_ShowMenu builds the menu, counts what it listed, and when it listed\nnothing adds one disabled row reading 'No buffs for this loadout yet' and\ndisplays it anyway. So a player carrying no buffed weapon gets a window over the\nscreen that exists only to say it has nothing to say.\n\nMvM_OnCommandKeyValues opens it on MvM_UpgradesBegin, so this happens every time\nthat player opens the upgrade station. Early in a run, before any buff has been\nfound, that is every player every time.\n\nIt costs more than the space: a SourceMod menu takes the number keys while it is\nup, and in MvM those are the weapon slots.\n\n## The rule\n\nNothing on screen when there is nothing to list. The empty case is not a menu.\n\nOne thing to keep: a player who typed the command deliberately still deserves an\nanswer, so that path says it in chat rather than opening an empty window. Only\nthe paths nobody asked for go silent.\n\nRelated: [[apw-pni]] is the same window arriving unasked at the station and\ntaking the number keys. That one is about when it opens, this one about whether\nit has anything in it, and fixing either does not fix the other.","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T13:55:08Z","created_by":"m-this","updated_at":"2026-08-27T14:49:50Z","started_at":"2026-08-27T14:48:42Z","closed_at":"2026-08-27T14:49:50Z","close_reason":"WeaponBuffs_ShowMenu no longer displays an empty menu: it returns false and frees the Menu when nothing was listed, and the disabled 'No buffs for this loadout yet' item is gone. Command_WeaponBuffs answers in chat on false. The MvM_UpgradesBegin path in MvM_OnCommandKeyValues now draws nothing and takes no number keys when the loadout has no buff.","comments":[{"id":"01a043b1-d3b2-73e4-8a1f-e26de4f3b2aa","issue_id":"apw-e0g","author":"m-this","text":"Found it: WeaponBuffs_ShowMenu adds a disabled 'No buffs for this loadout yet' row and displays the menu anyway. Callers: Command_WeaponBuffs (sm_ap_buffs, deliberate), Command_TestWeaponBuff, Command_GiveWeaponBuff (admin, always have at least one buff by then), and MvM_OnCommandKeyValues on MvM_UpgradesBegin (unasked). Plan: make ShowMenu return whether it displayed anything and never display an empty menu; Command_WeaponBuffs prints a chat line on false.","created_at":"2026-08-27T14:48:43Z"},{"id":"01a043b2-d75a-7706-90e1-0ff3f3b759aa","issue_id":"apw-e0g","author":"m-this","text":"Changed WeaponBuffs_ShowMenu to return bool: when listed == 0 it deletes the menu and returns false instead of adding the disabled 'No buffs for this loadout yet' row and displaying. Dropped that row entirely. Command_WeaponBuffs (sm_ap_buffs / !ap buffs) now prints '[AP] No buffs for this loadout yet.' via ReplyToCommand when the menu did not open, so the deliberate path still answers. The MvM_UpgradesBegin call in MvM_OnCommandKeyValues is left as is and now goes silent; added a one-line why-comment there. Command_TestWeaponBuff and Command_GiveWeaponBuff ignore the return, which is right: they only reach the call after granting at least one effect, so the menu always has a row. Not compiled: building is forbidden while a measurement run is in flight.","created_at":"2026-08-27T14:49:49Z"}],"dependency_count":0,"dependent_count":0,"comment_count":2}
{"_type":"issue","id":"apw-57p","title":"Bot Switcher shows a custom loadout as stock weapons","description":"Reported: 'i assigned bots with a custom loadout, and in the Bot Switcher bots\ntable it shows them with Stock Weapons'.\n\nbotlive.seatAt resolves the seat's loadout with class.LoadoutByKey, which only\nknows the presets this package ships. A custom key carries the custom: prefix\nand is unknown to it, and unknown is stock by design, so every custom loadout\nreads as stock weapons.\n\nThe table is the only place a player can check what the bots are about to carry,\nso it says the opposite of the truth about a setting they deliberately made. It\nis in all three surfaces, since the GUI tab, the TUI tab and !ap bots all print\nbotlive.Team.\n\nThe weapons the bots actually carry are right: the loadout file is written from\nLibraryOf(s), which does know the custom ones. This is the table lying, not the\nmod.","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T13:39:33Z","created_by":"m-this","updated_at":"2026-08-27T13:42:21Z","started_at":"2026-08-27T13:39:44Z","closed_at":"2026-08-27T13:42:21Z","close_reason":"Fixed, and the cause was one line: botlive.seatAt asked class.LoadoutByKey,\nwhich only knows the presets this package ships, so every custom key resolved\nto stock. It goes through LibraryOf(s).Loadout now, which is what writes the\nloadout file the mod actually reads.\n\nTwo tests, and both print 'Stock weapons' without the fix: one for a loadout on\na seat, one for a loadout given to the class that the seat inherits.\n\nOne fix covers all three surfaces, since the GUI tab, the TUI tab and !ap bots\nall print botlive.Team.\n\nThe same mistake was in the settings window's team Load and went with apw-5nv\nearlier today. Both are gone; LoadoutByKey now has no caller that can be handed\na custom key.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-u63","title":"The settings window will not open","description":"Reported 2026-08-27: the settings window no longer opens in the Windows\nlauncher. The terminal is unchecked.\n\nA launcher whose settings cannot be opened cannot be configured, so this is\nworse than any single setting being wrong: the room, the missions, the bots and\nthe reach are all behind that button.\n\n## What it is not\n\nIt builds and vets clean for GOOS=windows, so it is not a compile error. walk\nbuilds its dialogs at runtime and returns an error from Create, which\neditSettings logs as \"settings: ...\". That line is the first thing to read from\na debug bundle or the log tab.\n\n## What changed today, in the order it landed\n\nAny of these could be it, and all of them touch settings_windows.go:\n\n- the Loadouts sub-tab on the Bots page, a fourth page with its own editor\n- the Balancing page renamed from Rewards, with three NumberEdits added\n- kelly-cs's community map support, merged with one conflict resolved by hand\n  in this very file\n\nThe conflict is the first suspect, because it is the one place where two\nchanges met and a human chose. It was four lines: three robot levers against\nthe rename of pool.excluded to excludedMissions.\n\n## How to find it without a Windows machine\n\nThe error string names the widget walk refused. Failing that, the fastest cut is\nto build the dialog with each of the three changes reverted in turn.\n\nWorth checking first, because they are the cheap runtime failures walk has:\n\n- two widgets assigned to one AssignTo pointer\n- a ComboBox with an empty model and a Value that is not in it, which the new\n  saved-loadouts menu can produce when nothing is saved\n- a Grid whose ColumnSpan exceeds its Columns","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T12:48:02Z","created_by":"m-this","updated_at":"2026-08-27T13:02:12Z","started_at":"2026-08-27T12:48:10Z","closed_at":"2026-08-27T13:02:12Z","close_reason":"Fixed in 65abad6. The Balancing page's robot scales take a minimum of ten and a settings file written before those fields existed leaves them at zero, which walk refuses, taking the whole dialog with it. withDefaults now fills them. Verified the test catches it: disabled the fix and it fails with damage = 0, want 100.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-eei","title":"Mannhunt crashes far more than any other map","description":"Discord, 2026-08-27, k-kaneta:\n\n  \"ooh mannhunter in particular is VERY likely to crash\"\n  \"other map was fine after most recent patch. this one absolutely not\"\n  \"might be mission itself, might be map issue either way it crashes\"\n\nTwo things in that, and the second is as useful as the first: the other map he\nplayed was fine after the recent fixes, so whatever this is, it is not the\ngeneral crash that was going round.\n\n## What is already known about Mannhunt\n\nmvm-ipf: the engineer gets stuck inside a prop on this map. A wedged bot asks\nfor a path, is reset, comes back to the same wedge and asks again, and the\nwatchdog kills the server on that answer. That is measured on Mannworks and it\nis the same shape.\n\nv2.20.1 moves a bot that is wedged, after three stucks at one spot. Whether it\ncovers Mannhunt is unmeasured, and k-kaneta may have been playing a build\nwithout it.\n\n## What to do\n\nRun Mannhunt on the test-bed and read the crash the way run.sh now reports it:\nit says whether the watchdog killed the server or it segfaulted, and prints the\ncommand that turns the core into a stack. Those are different bugs and the\nreport tells them apart without guessing.\n\nIf it is the watchdog, look for the stuck line before it. If it is a segfault,\nthe core has the answer.","status":"in_progress","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T11:50:55Z","created_by":"m-this","updated_at":"2026-08-27T22:22:33Z","started_at":"2026-08-27T22:22:33Z","comments":[{"id":"01a04329-3b2f-7e75-9ed8-33085628c208","issue_id":"apw-eei","author":"m-this","text":"Confirmed by the reporter, and it is the engineer. This is the proof that the\nengineer AI causes the crashes.\n\n  omaewh4: \"Did you have an engineer in the team? Can you try without one?\n            they might be the source of those crashes, when they get stuck\"\n  k-kaneta: \"ok mannhunt works if there is no engie\"\n\nA map that crashes reliably with an engineer and does not crash without one is\nabout as clean a control as a play-test produces. It was a hypothesis from the\ntest-bed until now; a player removing one variable settled it.\n\nIt matches what was measured on Mannworks: a wedged bot asks for a path, is\nreset, comes back to the same wedge and asks again, and the watchdog kills the\nserver inside NavAreaBuildPath. mvm-ipf already records an engineer stuck inside\na prop on this very map, so the where is known as well.\n\n## What follows\n\nv2.20.1 moves a bot that is wedged after three stucks at one spot, and it took\nMannworks with Mean Machines from crashing at wave zero to four waves and no\ncrash. k-kaneta was almost certainly on a build without it.\n\nSo the next step is not more diagnosis. It is Mannhunt on the test-bed with the\ncurrent build and an engineer in the lineup: if it still crashes the recovery is\nnot catching this wedge, and the stuck line will say where.","created_at":"2026-08-27T12:19:31Z"},{"id":"01a0432a-b543-7b6a-a6e9-5b6880073cf4","issue_id":"apw-eei","author":"m-this","text":"Mannhunt is a community map, not one of Valve's. The test-bed has the seven\nValve MvM maps and nothing else, so reproducing this needs the community assets\nfetched first, through the support kelly-cs added in PR #2.\n\nThat is worth knowing rather than a detail:\n\n- it arrived with community map support, so it is the first crash report from\n  that feature\n- community nav meshes are not held to Valve's standard, which is the whole\n  reason kelly-cs needed the spawn recovery in tf2-mvm-bots#1 before the maps\n  were usable at all\n- a bot wedged in geometry is likelier on a map whose mesh nobody at Valve\n  signed off\n\nSo the wedge recovery in v2.20.1 is the right shape for it, and whether it\ncatches this particular geometry is still unmeasured.\n\nNext step, in order: fetch Mannhunt through the launcher's community assets,\nput it on the test-bed with an engineer in the lineup, and read the crash the\nway run.sh reports it. mvm-ipf holds the prop this map is known to wedge on.","created_at":"2026-08-27T12:21:08Z"},{"id":"01a04338-d187-7c06-b7ee-dc0703d4abe9","issue_id":"apw-eei","author":"m-this","text":"Correcting myself: Mannhunt is not a community map.\n\nIt is an Invasion mission on Mannworks, and Mannworks is Valve's. I searched the\ninstalled maps for a mannhunt.bsp, did not find one, and concluded the map was\nmissing. The map was never missing: the mission is a popfile on a map the\ntest-bed already has.\n\nSo everything I wrote about community assets and unvetted nav meshes is beside\nthe point, and worse, it pointed the next step at fetching something that does\nnot need fetching.\n\nWhat it actually means is better: Mannworks is the map I already reproduced the\nengineer wedge on, at 1014 885 274, and where the wedge recovery took a run from\ncrashing at wave zero to four clean waves. Mannhunt is a different mission over\nthat same geometry.\n\nSo this is testable now, on the build that has the fix.","created_at":"2026-08-27T12:36:32Z"},{"id":"01a04551-5786-7574-b90f-0b336c038865","issue_id":"apw-eei","author":"m-this","text":"Wording first: Mannhunt is a mission, mvm_mannworks_intermediate2, on the\nMannworks map. This bead's title says 'map' and that is wrong. The reports are\nabout the mission.\n\nThe evidence for the crash is strong, and it comes from k-kaneta's bundle of\n2026-08-27 rather than from a test-bed run. Three access violations, 0xc0000005.\nTwo were on Mannhunt, both at 1014 885 274, both wave 1, both after exactly\nthree engineer stuck cycles twelve seconds apart, crashing on the third. The\ncontrol is in the same log: at 21:13 Mannhunt loads again with no engineer on\nRED and clears waves 1, 2 and 3. That is k-kaneta's 'mannhunt works if there is\nno engie' with a coordinate attached.\n\nThe wedge is not always fatal. That coordinate appears ten times across five\ndifferent engineer bots and only twice does the server die, so the wedge is the\ncondition and something else decides.\n\nWhat cannot be done yet is verify any of it. [[mvm-ed0]] blocks this bead\ncompletely. Every Mannhunt run in the test-bed produces a wave where every\ncounter is zero for the whole timeout: no robot kills, no deaths, no damage, no\ncredits. No robot ever spawns and the timeout is recorded as a loss. A run that\nspawns no robots cannot crash on an engineer fighting them, so 'no crash in\nthree attempts' means nothing.\n\nTwo engineer fixes have shipped since that bundle, both real and both aimed\nhere: defenderbots v2.21.3 let the wedge recovery leave the area the bot stands\nin, and v2.22.0 let the watchdog see a bot with no behaviour at all. Neither has\nbeen shown to help on this mission, because this mission cannot be measured yet.\n\nDiagnostic queued: mvm_mannworks on two other missions, to say whether the stall\nfollows the map or the popfile.","created_at":"2026-08-27T22:22:34Z"},{"id":"01a0458d-9f96-7938-b3c6-224a41dc689b","issue_id":"apw-eei","author":"m-this","text":"A second fix, aimed at the crash rather than at the wedge, defenderbots v2.26.0.\n\nThe forced-wedge run said something the earlier runs could not. With\nsm_redbots_debug_wedge_seconds holding an engineer in place, twelve wedges\nproduced 36 stuck lines and not one successful recovery: MoveWedgedDefender was\nreached every time and refused every time, even with the v2.21.3 fix in.\n\nSo a bot the recovery cannot help is not rare, and the crash does not need the\nrecovery to work. What the cores show is the cost, not the wedge: a bot that\ncannot reach its goal asks for a path again on every reset, NavAreaBuildPath\nwalks the mesh for an answer that does not exist, and the watchdog kills the\nserver on that frame.\n\nAfter eight wedges the bot is now kicked, with his buildings, and the seat\nrefilled by the top-up path a moment later with a bot that can walk. Behind\nsm_redbots_feature_kick_wedged_bots, default off.\n\nThis is the belt to the recovery's braces. If the recovery is fixed later the\nkick should almost never fire, and its log line says when it does.\n\nWhy the recovery refuses is still open and now instrumented: v2.25.1 logs the\nwedge count and which of the two refusals MoveWedgedDefender took. That run is\nqueued behind the Mannhunt console capture.","created_at":"2026-08-27T23:28:24Z"}],"dependency_count":0,"dependent_count":0,"comment_count":5}
{"_type":"issue","id":"apw-6hf","title":"A refund gives back nothing when the credits came from a bundle","description":"Discord, 2026-08-27, Cowser: \"hitting the `-` button to return an upgrade\ndoesn't refund the credits if it was from a cash bundle.\"\n\napw-4cb closed on the granting half: a bundle is money the game never recorded,\nand the plugin pays it. This is the other direction. The upgrade station refunds\nfrom the game's own accounting, which never saw the bundle, so the credits go\nnowhere and the player is out both the upgrade and the money.\n\nWorse than not refunding: the player pressed a button that took something away\nand gave nothing back.\n\n## The rest of the same thread\n\nCowser: \"if a location is a cash bundle in a mission, and the server has to\nreset for some reason, it should be regranted after reobtaining it (currently\ndoes not afaik)\"\n\nk-kaneta: \"Cash bundle should be perma bonus across the entire mission tbh,\nmaybe reduce the amount to balance it out\"\n\nBoth are the same complaint from different sides: a bundle is a one-shot drop at\nthe moment it lands, so anything that resets the wave or the server loses it.\nomaewh4 has said as much: \"Cash bundle are hard to handle yeah... it's a known\nbug from the start of this project\".\n\nk-kaneta's suggestion is the design answer rather than a patch. A bundle held\nfor the mission, at a smaller number, cannot be lost to a reset because it is\nnot a drop any more. That is worth weighing against the refund fix, because one\nof them makes the other unnecessary.\n\n## Related\n\napw-onf, a restart making the team replay the mission, is the general case of\nthe same loss.","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T10:01:05Z","created_by":"m-this","updated_at":"2026-08-27T13:14:25Z","started_at":"2026-08-27T11:05:46Z","closed_at":"2026-08-27T13:14:25Z","close_reason":"Fixed in 3244741. The minus button refunds one upgrade and sends MVM_Upgrade with a negative count, not MVM_Respec, so it landed in the branch that only refreshed the weapon buffs. The sign now separates a sale from a refund; a refund restores the bundle ledger and a sale is left alone. Not verified in play: pressing the button needs a person at a station, and the station already logs any command nothing handles.","comments":[{"id":"01a04307-e83c-7119-abff-5e9c8e56d1b8","issue_id":"apw-6hf","author":"m-this","text":"Fixed in 3244741. The mechanism was not the one the code guessed at.\n\nMvM_OnCommandKeyValues already handled MVM_Respec and restored the ledger a\nframe later, and the comment above it flagged the weak point: \"The name is the\none thing here that is a guess.\"\n\nThe name was right. The button was not. Cowser pressed the minus button, which\nrefunds one upgrade rather than respeccing, and the station sends MVM_Upgrade\nwith a negative count for that. It was landing in the branch that only refreshes\nthe weapon buffs.\n\nSo the sign is what separates a sale from a refund, and the count is now read\nbefore the money side is told. A refund runs the same restore the respec does; a\nsale is left alone, because the game charges those against a balance that\nalready has the bundles in it and re-adding the ledger would mint money.\n\nNot verified in play. Pressing the minus button needs a person at a station, and\nwhat the station sends is still only documented rather than observed. The\nstation already logs any command nothing handles, so a run that still loses\ncredits will name the real one.\n\nThe other two halves of the thread are not this bead and are still open: a\nbundle lost to a server reset is apw-onf, and k-kaneta's suggestion of a bundle\nheld for the mission at a smaller number would make both unnecessary.","created_at":"2026-08-27T11:43:07Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"apw-onf","title":"A server restart makes the team replay the mission","description":"From play, 2026-08-26: the server stutters and crashes on Mannhattan, and when\nit comes back the team has the whole mission to do again.\n\n\"that should be NEVER happening\", and that is the right bar. A crash is a bug to\nfix; a crash that costs an evening's progress is a design fault on top of it,\nand this half can be fixed without fixing the crash.\n\n## What survives a restart today, and what does not\n\nThe bridge writes every check to disk, so the Archipelago half is safe: the\nunlocks and the items come back. bridge/internal/state holds seed, checks,\nitems, acked_seq, goal_sent and the last check.\n\nNothing holds where the team actually was. SrcdsStartMission is the popfile the\nserver loads first, and its own comment says it is \"never written again\". So a\nrestarted server loads the mission the settings name, at wave 1, whatever the\nteam had cleared.\n\nThat is the whole of \"everything to redo\": the mission they were playing and\nevery wave they had won inside it.\n\n## The fix has a mechanism already\n\ntf_mvm_jump_to_wave is a cheat command and the test-bed already drives it\n(testbed/run.sh:225). It grants the credits of the waves it skipped, which is\nexactly what a team arriving at that wave would have held, so the restore does\nnot hand anybody a wave they have to play underfunded.\n\nSo the shape is:\n\n1. The plugin knows the popfile and the wave. Write both somewhere durable\n   whenever a wave completes, next to the checks rather than in the settings:\n   the settings are what somebody configured, and this is what happened.\n2. On start, if that record names a mission the run has unlocked, load it and\n   jump to the wave after the last one cleared.\n3. Say so in the chat, or a player who crashed mid wave 4 cannot tell a restored\n   run from a lost one.\n\n## What to be careful about\n\n- Only restore forward. A record that names a wave the team never reached is a\n  way to skip content, so the wave written is the one after the last completed\n  and nothing else.\n- The jump needs sv_cheats and puts it back. A server left with cheats on is\n  worse than a lost mission.\n- A finished mission clears the record, or the next start drops the team into\n  the end of a mission they already beat.\n- Test mode and a real room both need it, because a crash does not care.\n\n## Related\n\nmvm-cf3 in tf2-mvm-bots is the crash and the stutter, now P0 as well. This bead\nis the cost of the crash and is worth doing whether or not that one lands.","status":"in_progress","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T06:45:21Z","created_by":"m-this","updated_at":"2026-08-27T11:43:17Z","started_at":"2026-08-27T11:43:17Z","comments":[{"id":"01a0435b-b9f6-7225-8ebf-ab3f78f0b31d","issue_id":"apw-onf","author":"m-this","text":"Half built, and this is the half line.\n\nDone, in c8b622f and after it: the bridge keeps a Resume record beside the\nchecks, moves it forward on a cleared wave, clears it on a cleared mission, and\nserves it on the /missions answer the plugin already asks for when it starts.\nFour tests on the record and one on the endpoint.\n\nNot done: the plugin does not read it yet. So a crash still costs the mission,\nbecause nothing puts the team back.\n\nWhat is left, and the order:\n\n1. read Resume from the missions answer the plugin already fetches\n2. if it names a mission the run has unlocked and a wave above one, load that\n   popfile and jump to the wave after the last one cleared\n3. say so in the chat, or a player who crashed mid wave four cannot tell a\n   restored run from a lost one\n\ntf_mvm_jump_to_wave is the mechanism and testbed/run.sh:225 already drives it.\nIt grants the credits of the waves it skipped, which is what a team arriving at\nthat wave would have held.\n\nTwo things to get right, both noted when this was filed: only ever restore\nforward, and put sv_cheats back after the jump.","created_at":"2026-08-27T13:14:40Z"},{"id":"01a043b3-a831-77da-91ec-98c6433193b9","issue_id":"apw-onf","author":"m-this","text":"Plugin half not started: the agent sent at it was given a worktree of tf2-mvm-bots, not of this repository, so plugin/scripting/tf2_archipelago/ was readable but not writable. No edit made.\n\nThe design it got to, for whoever picks it up:\n\n- missions.inc keeps the record (popfile, last wave cleared) plus a consulted flag, and acts on it once per plugin load. The bridge moves the record forward all evening; read again at the third map change it is not a restore, it is the plugin dragging the team off where they are.\n- one line in bridge.inc OnMissions, before Missions_OnListUpdated(), hands the payload to a Missions_NoteResume() that reads the optional resume object. The bridge put resume on /missions on purpose, so no second fetch.\n- loading the mission is a map change, so the plugin asks once and then waits on a one-second timer, bounded, until MvM_PopFile() reports the mission. The switch guard returns early while a switch is in flight, so OnListUpdated alone never sees the arrival and cannot be the only driver.\n- forward only, two tests, not one: the target is last-cleared+1, refused if MvM_WaveFromGame() is already at or past it, and refused if it is past mission.waves.\n- sv_cheats goes back to the value it had, not to 0 as testbed/run.sh does. The three ServerCommands queue in order, so cheats are on for the jump and off again in the same frame.\n\nUntestable without a server either way: nothing here can be shown to work by compiling, only by a restart mid-mission on a live server.","created_at":"2026-08-27T14:50:43Z"},{"id":"01a043b8-1746-7e81-a97c-65aa96f7cedb","issue_id":"apw-onf","author":"m-this","text":"Plugin half written, compiles clean with spcomp 1.12.0.7246 (make plugin). Not verified on a server: nothing here can be exercised without a real MvM map, so the bead stays in_progress.\n\nWhat landed, two files:\n\nbridge.inc, OnMissions: reads the optional resume object off the /missions payload it already has and calls Missions_NoteResume(popfile, wave) before Missions_OnListUpdated(). No second fetch.\n\nmissions.inc, at the end: the record, a one-second repeating timer capped at 120 ticks, the forward-only checks and the jump.\n\nThe shape, and why it is not driven from Missions_OnListUpdated: that returns early while a switch is in flight, and after the changelevel nothing re-fetches the list, so at the moment the mission finally lands there is nobody left to jump. So NoteResume loads the mission itself (before OnListUpdated gets a look at the list, so g_SwitchingPopFile is already set and the guard leaves it alone) and a timer waits for the popfile to come up. The timer has no TIMER_FLAG_NO_MAPCHANGE on purpose: it has to outlive the changelevel it just asked for.\n\nForward-only, three refusals: the record is dropped if wave \u003e= mission.waves (nothing left to go back to), the target wave+1 is refused if it is past mission.waves, and refused if MvM_WaveFromGame() is already at or past it. Consulted once per plugin load, so the bridge moving the record forward all evening is not a second restore.\n\nsv_cheats: FindConVar reads the current value, then all three go through ServerCommand in order (sv_cheats 1, tf_mvm_jump_to_wave N, sv_cheats \u003cprevious\u003e). Setting the variable back through the ConVar handle would land before the queued jump ran, and the value restored is the previous one, not zero.\n\nChat: one line when the mission reloads, one when the jump lands naming the wave and how many were cleared.\n\nOne guess I could not measure: MvM_PopFile falls back to the map name when the objective resource will not answer, so a mission whose popfile is its map name could read as loaded while the map is still coming up. The timer waits while g_PendingPopFile is non-empty to cover it. Worth a look on a live restore.\n\nLeft to verify on a server: that tf_mvm_jump_to_wave through the command queue actually lands with sv_cheats set, that the credits of the skipped waves arrive, that sv_cheats reads back at its previous value after, and the whole path from a real crash.","created_at":"2026-08-27T14:55:33Z"},{"id":"01a043b9-5c28-7c92-8f74-a8bb4f176a73","issue_id":"apw-onf","author":"m-this","text":"Plugin half is in, and the bead stays in_progress because none of it has met a\nserver.\n\nbridge.inc OnMissions reads the resume object off the payload it already holds\nand calls Missions_NoteResume before Missions_OnListUpdated, so there is no\nsecond fetch. missions.inc gained Missions_NoteResume, Timer_Resume,\nMissions_Resume and Missions_JumpToWave.\n\nThree things worth keeping:\n\n- it is not driven from Missions_OnListUpdated, which returns early during a\n  switch and is never re-fired after a changelevel. NoteResume loads the mission\n  itself, before the list is seen.\n- the retry is bounded: 1s repeating, hard cap 120 ticks, then an error and\n  stop. No TIMER_FLAG_NO_MAPCHANGE on purpose, since it has to outlive the\n  changelevel it just asked for.\n- forward-only ended up as three refusals, not two: the record is dropped when\n  wave \u003e= mission.waves, wave+1 is refused past mission.waves, and refused when\n  the game is already at or past it.\n\nsv_cheats goes back to its previous value, and all three commands go through\nServerCommand in order: restoring through the ConVar handle would land before\nthe queued jump ran.\n\nCompiles clean. What a server still has to say: that the jump lands with cheats\nset from the queue, that the skipped waves' credits arrive, that sv_cheats reads\nback at its old value, and that MvM_PopFile does not read as loaded mid\nchangelevel on a mission whose popfile equals its map name.","created_at":"2026-08-27T14:56:56Z"},{"id":"01a043bc-5476-7dd0-8ffa-81098b1d791d","issue_id":"apw-onf","author":"m-this","text":"Queued for verification behind the test-bed, which is one server and currently\nmeasuring apw-d7u.\n\nWhat the run has to show, in order:\n\n1. clear a wave on a mission, kill the server, start it again, and watch the\n   plugin reload the popfile and land on the wave after the last cleared one\n2. sv_cheats reads back at whatever it was before the jump, not 0\n3. the skipped waves' credits arrive\n4. a second restart on the same mission does not send the team backwards\n\nUntil that has happened this stays in_progress. The code compiles and the logic\nis argued, which is not the same as working.","created_at":"2026-08-27T15:00:11Z"}],"dependency_count":0,"dependent_count":0,"comment_count":5}
{"_type":"issue","id":"apw-r85","title":"The game server crashes with an access violation, and the launcher reports it as the bridge stopping","description":"k-kaneta's second debug bundle, Windows, launcher 1.9.0-c42e61c, room on\nlocalhost. Twice in two sessions:\n\n    19:29:21  launcher game server stopped: exit status 0xc0000005\n    21:09:26  launcher game server stopped: exit status 0xc0000005\n\n`0xc0000005` is `STATUS_ACCESS_VIOLATION`. srcds is not exiting, it is\ncrashing.\n\n## The reporting half is done\n\nk-kaneta described it as \"bridge stopping then disconnecting\", because the\nlauncher printed its own orderly shutdown on the way out and that was the last\nline he saw. `wrapExit` now names a crash a crash: an NTSTATUS exception or a\nfatal signal reads as `game server CRASHED: ... (access violation, so this is a\ncrash and not a stop)`, and a plain non-zero exit still reads as a stop, so a\nmap srcds refuses does not send anybody looking for a minidump.\n\nThe debug bundle's `summary.txt` also carries a \"What looks wrong\" section now,\nwhich lists crashes with the status quoted. Anybody reading the next bundle\nsees this at the top of the first file they open.\n\n## The crash itself is still open, and needs the dump\n\n`Using breakpad crash handler` appears at every srcds start, and\n`newestCrashDumps` already collects `.mdmp` files from the game directory and\nits parent into `crashes/` in the bundle. k-kaneta's bundle has no `crashes/`\nfolder despite two crashes, so either srcds wrote no dump or it wrote one\nsomewhere that lookup does not search.\n\nSettle that first, because it decides everything after:\n\n- Ask k-kaneta whether any `.mdmp` exists under the install root.\n- If one does, find where, and widen `newestCrashDumps`. It currently checks\n  only `\u003croot\u003e/tf-dedicated/tf` and `\u003croot\u003e/tf-dedicated`, and matches only the\n  `.mdmp` suffix.\n- If none does, the summary should say so plainly when it finds a crash and no\n  dump, rather than leaving a reader to notice the absence.\n\nTwo things in the log immediately before the 21:09:26 crash, neither confirmed\nas related:\n\n- `Robot's getting stuck with worldspawn`, nine times in the four seconds\n  before it.\n- `[defenderbots] stuck: SomeDude (engineer) ... for 12s` three seconds before\n  it, which is mvm-6rt's Engineer.\n\nThe earlier crash at 19:29:21 is in `launcher-previous.log` and should be\nchecked for the same two.\n\nNote this is a Windows bundle. Whether native Linux crashes the same way is\nopen, and worth asking, since apw-4cb deliberately turned the watchdog off\nthere.","design":"## Still open, and now it can be diagnosed when it next happens\n\nTwo halves were done and the crash itself is untouched, because it cannot be\nfound without a minidump.\n\n**Reporting**: `wrapExit` names an NTSTATUS exception or a fatal signal as a\ncrash rather than a stop, so \"bridge stopping\" stops being the last thing a\nplayer sees.\n\n**Collection**: the dump search was looking in two directories for one suffix.\nIt now takes `.dmp` as well as `.mdmp`, and searches the install root and a\n`CrashDumps` folder under any of the three places, not just the game directory\nand its parent. And when the logs hold a crash but no dump turns up,\n`summary.txt` says so and says where to look, instead of reading like a run that\nnever crashed.\n\n**Ruled out**: the engine watchdog, see apw-7v6. It does not exist on Windows,\nso it cannot have fired.\n\n## What is left\n\nThe crash. It needs a bundle from a run that crashed, taken with a launcher\nbuilt after this change, so the dump is either in `crashes/` or its absence is\nstated. Ask k-kaneta, whose two crashes are the only ones recorded.\n\nIf the next bundle still says NO CRASH DUMP, that is itself the finding: srcds\nruns under Breakpad and should write one, and a machine where it does not needs\nthat answered before the stack is reachable.\n\nThe two log lines that preceded the crash are still unexplained and still\nunconfirmed as related: nine `Robot's getting stuck with worldspawn` in four\nseconds, and a `[defenderbots] stuck:` engineer three seconds before it.","status":"deferred","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T13:18:03Z","created_by":"m-this","updated_at":"2026-08-26T21:19:55Z","started_at":"2026-08-26T17:08:58Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-2sw","title":"The TUI never rewrites server.cfg, so bot settings never reach the server","description":"Split out of apw-ygn, which is where the evidence came from. This is the cause\nof that bead's second half, and it is worth its own bead because it is not\nabout the blacklist: it drops *every* setting that lives in `server.cfg`.\n\n## The gap\n\n`srcdsconfig.Install` has exactly two call sites:\n\n- `internal/gui/gui_windows.go:427`, on the Windows GUI's start path, right\n  after the installer and before the server comes up. Correct.\n- `cmd/tf2ap/main.go:148`, in `guided()`, **once**, before `tui.Run` is\n  reached.\n\n`internal/tui/` never calls it. Not on save, not on start, not on restart.\n\nSo on the TUI path `server.cfg` is written once, at launcher startup, from\nwhatever the settings were then. Every edit made in the TUI afterwards is saved\nto `config.json` and never rendered. The running server keeps the file it was\ngiven, for the life of the launcher process.\n\n`applySettings` in `internal/tui/tui.go:219` even restarts the server for this\nexact reason. Its own comment:\n\n\u003e The server reads its half at startup, so a change while it is running is a\n\u003e change the running server does not have: the window restarts it for that\n\u003e reason, and so does this.\n\nThe restart is right and the reasoning is right. It just restarts the server\nonto a stale `server.cfg`, so it changes nothing that the file controls.\n\n`resetSettings` at line 200 has the same gap: it writes defaults to disk and\nhands them to the supervisor without re-rendering.\n\n## Why it is P0\n\nEvery convar in `server.cfg.tmpl` is affected, not only the bot classes:\nhostname, `sv_password`, the team size, the lineup, the loadouts, the hats, the\nstart mission. A player editing any of them in the TUI gets a saved value, a\nUI showing the new value, a server restart, and no change. Nothing anywhere\nsays the setting did not land.\n\nIt is also platform-split, which is how it survived this long: Windows testers\ngo through the GUI, which is correct. Linux and `-tui` users get none of it.\n\n## Evidence\n\nCowser's bundle, on native Linux, running the TUI. `config.json`:\n\n    \"srcds_bot_class_blacklist\": [\"spy\"]\n\nThe `server.cfg` generated for that same run:\n\n    sm_redbots_manager_class_blacklist \"\"\n\n`botloadout.Blacklist([\"spy\"])` returns `\"spy\"` — I ran it against his settings.\nSo the launcher never rendered that empty string from this config. It rendered\n`server.cfg` before he unticked the Spy, and then never again.\n\nThis also rules out the settings checkboxes being at fault. His `config.json`\nholds the blacklist, so the TUI toggle wrote it correctly. The break is\ndownstream of the checkbox.\n\n## Fix\n\nRender the configs whenever the server is about to start, the way the GUI\nalready does, rather than once per launcher run. The start path is the right\nplace for it, not the save path: that covers save-then-start, the restart in\n`applySettings`, and `resetSettings`, with one call instead of three.\n\nCheck whether `runtime.Run` is the better home. Both front ends call it, and a\nrender there would make the guarantee \"a server that starts, starts from the\ncurrent settings\" true for every caller, including a future one.\n\n## Test\n\nStart the TUI, untick a class, confirm the rendered `server.cfg` on disk\nchanges. Then the reverse: check that nothing rewrites the file while a server\nis running and reading it.","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T13:08:43Z","created_by":"m-this","updated_at":"2026-08-26T13:42:25Z","closed_at":"2026-08-26T13:42:25Z","close_reason":"Fixed. Supervisor.Start and runtime.Run both call srcdsconfig.Install, so both front ends and console mode render server.cfg from the settings that start is using. The redundant call on the Windows start path is gone, and srcdsconfig.RenderServerCfg is extracted so the file can be compared without being written. TestStartRendersTheConfigsFromTheCurrentSettings covers it and fails without the fix.","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"apw-bl6","title":"`tip` moving tag like Ghostty, so that release has always a fresh release candidate","description":"Do the same as Ghostty GitHub repo, it has a moving tag named \"tip\" that is in fact the next release candidate ! its cool for me to be able to test out the new stuff, prepare the changelog for next release, etc.","status":"closed","priority":0,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-26T09:47:58Z","created_by":"omae-beads","updated_at":"2026-08-26T11:24:41Z","closed_at":"2026-08-26T11:24:41Z","close_reason":"Published and verified. The rolling prerelease is nightly, not tip: GitHub reserved the name tip permanently when the first run published a release against it under immutable releases, and neither deleting the release nor disabling the feature gives a reserved name back.","labels":["Forgejo","GitHub"],"comments":[{"id":"01a03d7a-dc01-7971-b9ee-f7b854ef68a7","issue_id":"apw-bl6","author":"m-this","text":"Done in .github/workflows/tip.yml, with a note in the README under the Linux\ndownload.\n\nHow it works. Every push to main builds the same files a release builds, deletes\nthe old tip release, force-moves the tip tag to the new head and publishes a\nprerelease off it. Concurrency group tip with cancel-in-progress, so a second\npush wins rather than racing the first: two of these would both force-move the\nsame tag and the loser would leave the release describing a commit the tag no\nlonger points at.\n\nFour decisions worth knowing about, because each one could have gone the other\nway.\n\nThe release is deleted before the tag moves. A release is bound to a tag name,\nso moving the tag under a live release leaves it describing a commit that is not\nthere any more. Deleting first turns that window into one where tip is absent\nrather than one where it is wrong. --cleanup-tag is deliberately not used: it\nwould delete the ref the job is about to move, and other clones keep a deleted\nref.\n\nThe images are tagged tip and never latest. The release job moves latest on\nevery tag because latest is what `make up` should get; doing that here would put\nevery push to main on anybody running the published stack.\n\nThe notes are the commit list since the last real tag, found with git describe,\nwhich is why this job checks out deep where the release job checks out shallow.\nThat list is the raw material for the next CHANGELOG.md section, which was half\nthe ask.\n\nNo version-check, no signing, no VirusTotal. make version-check asks whether a\ntag agrees with the version in the tree and tip disagrees with every tree by\ndesign. The SignPath policy is release-signing and this is not a release, and it\nis blocked anyway (apw-gb8). A scan of a binary replaced several times a day\nspends the quota the tags people actually download need. Attestation is kept:\nit is the only check anybody has on an unsigned exe, and a tip build is exactly\nthe file with no reason to be trusted.\n\nOne thing that had to be written rather than reused: .github/release-notes.md\npoints its download badges at releases/latest/download/, and latest excludes\nprereleases, so reusing it would have shipped dead links. The tip notes link\nreleases/download/tip/ directly.\n\nNot done on Forgejo. The job carries the same `github.server_url` guard the\nrelease job does, because ghcr.io, the release API and the gh CLI are all\nGitHub's. Forgejo has releases and could host this, but it needs its own\nregistry and runner story rather than the same file.\n\nWorth knowing about the cost: this is a full cross-compile plus three Docker\nimage pushes on every push to main. The cancel-in-progress keeps a burst down to\none build, but it is not free.","created_at":"2026-08-26T09:50:57Z"},{"id":"01a03dbe-6c23-7871-a25c-4c22e82306d1","issue_id":"apw-bl6","author":"m-this","text":"Reopened: the workflow is right and the repository will not let it run.\n\nA ruleset on this repo covers every tag, and it refuses both halves of what a\nmoving tag needs. Force-moving gives \"GH013: Cannot update this protected ref\".\nDeleting and recreating gets past the delete and then gives \"Cannot create ref\ndue to creations being restricted\". There is no order of operations that works\nunder it.\n\nThe very first tip run did succeed, on 2026-08-26 at 09:53, which is worth\nknowing: the ruleset either did not cover tags then or was added after. So the\nworkflow itself is proven to work end to end. Everything before the tag step\nruns green: three images pushed to ghcr, make dist, the notes, the checksums and\nthe attestation.\n\nThe fix is a settings change and it cannot be made from a checkout. The tag\nruleset's target moves from all tags to refs/tags/v*, so it goes on protecting\nthe release tags and stops catching tip. Chosen over adding an Actions bypass,\nwhich would let any workflow touch any release tag to solve a problem only tip\nhas.\n\nTwo things were fixed on the way here and both are real, so they stay:\n\n  - the tag is deleted and recreated rather than force-moved (813f90b)\n  - the local tag is dropped first (ef54cff). This job checks out at\n    fetch-depth 0 so git describe can find the last release, which fetches\n    every tag including tip, and `git tag tip` then dies with \"already exists\"\n    before the remote is touched. The release job does not have this problem\n    because it checks out shallow.\n\nState right now: no tip tag and no tip release, because the last run deleted the\nold one before failing to make the new one. The v* release tags are untouched.\nRe-run the workflow once the ruleset is scoped and it should publish.","created_at":"2026-08-26T11:04:45Z"},{"id":"01a03dd0-b372-7a92-89d1-357ab4534425","issue_id":"apw-bl6","author":"m-this","text":"Working, verified on run 32962821388.\n\n  tag        refs/tags/nightly at 7d9b9ac, which is the head of main\n  release    prerelease, 11 assets\n  notes      the not-a-release warning, the two download links, and a\n             \"Since v1.9.0\" section listing every commit\n  images     bridge, srcds and archipelago pushed to ghcr as :nightly, never\n             :latest\n\nFour failures got here and each one taught something worth keeping.\n\n1. A force-update of the tag is refused. Changed to delete and recreate, which\n   reverses the argument the first version of this file made against\n   --cleanup-tag. That argument was right about the cost and wrong about the\n   priority.\n2. `git tag nightly` then died locally with \"already exists\". This job checks\n   out at fetch-depth 0 so git describe can find the last release, and that\n   fetches every tag. The release job checks out shallow and never sees it.\n3. Creation was refused too, which no visible ruleset explained. The \"protected\n   tags\" ruleset is scoped to refs/tags/v*, carries deletion, non_fast_forward\n   and update, and has no creation rule: it was never the cause.\n4. The cause, found by pushing tags by hand rather than reading settings: a\n   throwaway tag created and deleted cleanly, and tip was refused seconds later\n   from a local SSH identity as well as from Actions. Name-specific and\n   identity-independent, which only immutable releases explains. It locks a tag\n   the moment a release is published against it, and the lock outlives both the\n   release and the feature.\n\nThe thing to remember: leave immutable releases off, or scope it to\nrefs/tags/v* where it belongs. Switched on repo-wide, the next run here burns\nnightly the same way and the fix is another rename.\n\nUnrelated and pre-existing, so not touched: the release asset is named\ndefault.env.example rather than .env.example, on v1.9.0 as well as here. gh\nrenames a leading-dot file on upload. Worth its own bead if the name matters to\nanybody following the setup docs.","created_at":"2026-08-26T11:24:43Z"}],"dependency_count":0,"dependent_count":0,"comment_count":3}
{"_type":"issue","id":"apw-o75","title":"The goal read a check somebody else made","description":"A play-tester was told their run was complete having beaten three of the five\nmissions they drew. Another player in the room finished, ran `!collect`, and\nArchipelago checked every location holding that player's items, mission clears\namong them. The bridge adopts the room's checked list on connect, which is what\nmakes a lost state file survivable, and it read the win off that same list.\n\nFixed by keeping the two apart. `Played` is the locations this server checked\nitself, only the plugin writes it, and the goal is read off it. `Checks` still\nholds everything the room says, because that is what `!checked` and a recovered\nstate file are for. State format 3; a file written before it takes its checks as\nplayed, since the run in it was played by somebody.\n\nWhat is still open is the report's own suggestion, and it is the better shape:\n\n- Lock a trophy item onto every mission clear, an **Australium Medal**, so no\n  other player's item is ever under one and `!collect` cannot touch it. The goal\n  becomes `state.has(\"Australium Medal\", player, target)` rather than\n  `can_reach_location`, which is what generation logic wants anyway.\n- It costs the multiworld eight locations' worth of other people's items, which\n  is a real loss: a mission clear is one of the better checks this world offers.\n- It needs an item id, so it is a gamedata change on the Go side, a regenerated\n  `data.py`, and a data format bump. That is why it waits for the next time the\n  format moves rather than going out on its own.","notes":"Imported from TODO.md item 7 — section: Bugs, most impactful first\nOriginal heading: The goal read a check somebody else made. Fixed in the bridge, open in the apworld\nPeppy has play-tested the fix and confirms the goal half: \"it doesnt actually\ncomplete your own goal on itself anymore it seems so thats good\".\n\nThe display half is not fixed and is now apw-4bt: the mission list still reads\nchecks, so another player's !collect still draws as your clear.\nThe Australium Medal half is split out to apw-768. It is a design change with a\ndata format bump behind it rather than the tail of this fix, and holding this\nbead open for it hid that the bug itself is dealt with.\n\nWhat is left here is the display half, which is apw-4bt.","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T13:53:00Z","created_by":"m-this","updated_at":"2026-08-26T08:33:03Z","closed_at":"2026-08-26T08:33:03Z","close_reason":"The bug is fixed and play-tested. Played and checks are kept apart, the goal reads played (client.go:369), state format 3 migrates an older file by taking its checks as played, and client_test.go:477 fails if the goal is ever announced off an adopted check again. Peppy confirmed it in play on 2026-08-26. The two things it carried are their own beads now: the display half is apw-4bt, the Australium Medal is apw-768.","labels":["todo-import"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-4cb","title":"A bundle is money the game never recorded","description":"Two reports, one cause. Spend a bundle on upgrades and lose the wave, and the\nbalance goes negative. Receive bundles, spend them, press refund, and the refund\nhands back the standard 400: \"on wave start I had like 1200 after receiving\nbundles, spent it, and then clicked refund I had the standard 400.\"\n\n`MvM_GrantCredits` writes `m_nCurrency` straight onto the player. That puts a\nnumber on the screen that the game's own bookkeeping never saw, and both of\nthose buttons read the bookkeeping rather than the number. The refund does not\ngive back what was spent: it restores the balance the game recorded at wave\nstart, which was 400 because the 800 in bundles was never part of it.\n\nHanding the money over the way the game does was the plan, and the pack is out.\nThree attempts on a live server:\n\n- `SetEntProp(pack, Prop_Send, \"m_nAmount\")` throws. A throw aborts the whole\n  callback, so the bundle was never paid, never acknowledged, and asked for\n  again on the next poll: eighty exceptions and two hundred and forty dead packs\n  in ten minutes.\n- `Prop_Data` for the same name is not there either. That one said so once a\n  bundle and paid nobody, which is the spam a play-test reported.\n- A probe over `m_nAmount`, `m_nCurrencyAmount` and `m_iAmount`, both tables,\n  answered on 2026-08-22: *\"A currency pack here has none of the amount\n  properties.\"* `item_currencypack_custom` cannot be told what it is worth\n  through a property, and that route is closed.\n\nSo the money keeps going on with `m_nCurrency`, and the bookkeeping is kept\nhere instead. `g_BundleCredits` is a per-client ledger of everything a bundle\never added, cleared on a map change. The station's refund command is caught and\nthe ledger is put back on top of the balance the game restored, one frame\nlater, because the game writes that balance during the command.\n\nWhat is left:\n\n1. The refund command's name is a guess: `MVM_Respec`, which is what the\n   community documents the button as sending. Any other command the station\n   sends is now written to the log once, so the next debug bundle either shows\n   nothing (the guess was right) or names the real one.\n2. The lost wave is covered by the same ledger, on `mvm_wave_failed`, and a\n   balance that would still come back negative is put at zero. Watch a debug\n   bundle for the line saying what each player came back to: if the game\n   restores after the event rather than during it, the frame's delay is not\n   enough and it wants a short timer instead.\n3. `CTFGameRules::DistributeCurrencyAmount` is what the game calls itself and\n   would make the ledger unnecessary. It wants a signature in our own gamedata\n   for two platforms and upkeep across every Team Fortress 2 update, which is\n   why it stays last.","design":"## Where this actually stands\n\nNot in progress. Left marked so from an earlier session, and nobody has worked\nit since.\n\nThe credit-pack route was tried and abandoned: `m_nAmount` is neither a send\nproperty nor a data property on this server, the first spelling threw eighty\ntimes in ten minutes and left a dead pack behind each time, and the second said\nso once a bundle and paid nobody. `mvm.inc` records that in full above\n`MvM_DropCredits`.\n\nSo the property write is back and it is what pays. `MvM_GrantCredits` writes\n`m_nCurrency` for every defender, humans and bots alike, which is the half that\nworks and the half apw-1w4 needed.\n\n**The reported bug is untouched.** The comment in the file says as much: writing\n`m_nCurrency` \"is what the refund and the wave-loss restore do not know about:\nthat is TODO item 1, still open\". A bundle spent and then refunded still hands\nback the 400 the game recorded, and a bundle spent on a lost wave still goes\nnegative.\n\n## The thing to find\n\nWhat the game restores from. The refund and the wave-loss restore both read the\ngame's own bookkeeping, so the fix is either to write the bundle into that\nbookkeeping instead of onto the player, or to hook both restores and add the\nbundle back on top. Which is possible depends on what that bookkeeping is and\nwhether it is reachable, and nobody has looked yet.\n\n`tf_upgrades_shared.cpp` and the client panel in\n`c_tf_upgrades.cpp` are in Source SDK 2013 and are the obvious read. Read them,\nnever copy them: apw-48u carries the same caveat and the same licence reason.","notes":"Imported from TODO.md item 1 — section: Bugs, most impactful first\nOriginal heading: A bundle is money the game never recorded. Half fixed","status":"closed","priority":0,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T13:52:59Z","created_by":"m-this","updated_at":"2026-08-26T21:18:36Z","started_at":"2026-08-25T13:53:42Z","closed_at":"2026-08-26T21:18:36Z","close_reason":"Closed","labels":["todo-import"],"comments":[{"id":"01a03933-add9-7b9f-affc-d85a23d405bd","issue_id":"apw-4cb","author":"m-this","text":"tf2-mvm-bots' TODO flagged this as not its own: the plugin writes m_nCurrency directly, so the game's wave-loss restore never knew the money existed.","created_at":"2026-08-25T13:54:43Z"},{"id":"01a03ced-4c03-7adf-ac93-7708282fd250","issue_id":"apw-4cb","author":"m-this","text":"Folded in from apw-4i1, which was filed before this bead was read and is now\nclosed as a duplicate. The half it had that this one does not:\n\nThe bots are paid nothing at all. MvM_GrantCredits loops the clients and pays\neach one that passes MvM_IsPlayer, and that predicate ends in\n!IsFakeClient(client). In most runs five of the six RED seats are bots, so most\nof every bundle goes nowhere. That is separate from the bookkeeping being wrong;\neven once the ledger is right, the bot team still receives nothing.\n\nDo not widen MvM_IsPlayer to fix it. It is load-bearing where it is:\nunlocks.inc uses it to keep class and slot enforcement off the robots, and\ntf2_archipelago.sp:459 says so outright, \"Both events fire for robots too;\nMvM_IsPlayer is what leaves their loadout alone\". The payout wants its own\npredicate for a RED defender, human or bot. MvM_CanPayCredits should keep asking\nabout humans, because it is asking whether the moment is payable rather than who\nis owed.\n\nThis gets easier now that the pack route is closed and the money goes on with\nm_nCurrency: that write works on a fake client the same as on a human. What it\nmeans for the plan above is that g_BundleCredits has to be a ledger for bots\ntoo, and that both the refund catch and the mvm_wave_failed restore have to walk\nthem.\n\nOne thing that will undo it if it is missed. The defender mod sets a bot's\ncurrency wholesale whenever it spawns, in its events.sp:\n\n    TF2_SetCurrency(data, GetStartingCurrency(g_iPopulationManager)\n                          + GetAcquiredCreditsOfAllWaves())\n\nNeither term knows about bundles, so any bot that respawns is reset to a number\nwith no bundle money in it, and the ledger here is overwritten from the other\nside. That formula has to add the ledger's total or the fix leaks on every\nrespawn. Tracked from the other repo as mvm-6dc, which needs the same total for\nmid-mission class changes.\n\nDone when a run with several bundles granted shows each bot's balance matching a\nhuman's to the credit, across a wave loss, a refund, and a respawn.","created_at":"2026-08-26T07:16:20Z"},{"id":"01a03d32-02cb-73d1-acac-6ceebd6ba1e7","issue_id":"apw-4cb","author":"m-this","text":"The bot half of the folded-in comment is done. MvM_IsDefender is a new predicate\nnext to MvM_IsPlayer, in-game and on RED, human or bot. MvM_GrantCredits pays\ndefenders and MvM_RestoreBundlesAll restores them, and MvM_IsPlayer is untouched\nso unlock enforcement still leaves a bot's loadout alone. Compiles clean.\n\nOne thing is deliberately NOT done, because doing it by feel would have been\nwrong.\n\nThe defender mod writes a bot's currency wholesale on join, in its events.sp:\n\n    TF2_SetCurrency(data, GetStartingCurrency(g_iPopulationManager)\n                          + GetAcquiredCreditsOfAllWaves())\n\nNeither term knows about bundles, so a bot that rejoins comes back without them.\nThe obvious patch is to re-run the ledger restore on player_spawn for a bot, and\nit is wrong: Frame_RestoreBundles adds the whole ledger on top of whatever the\ngame currently holds, which is only safe after something has restored the game's\nrecord. A bot dying and respawning mid-wave is not that, and re-adding there\ninflates the balance on every death.\n\nSo the restore stays where it is, tied to the two events that really do reset\nthe record: the refund command and mvm_wave_failed. What the rejoin case needs\nis one of\n\n  a. the defender mod asking this plugin for the ledger and adding it to that\n     sum, which means a native across the two plugins, or\n  b. this plugin detecting the reset rather than assuming it, by comparing the\n     held balance against what the ledger says it should be, which is guessing\n     from a number two plugins both write, or\n  c. the defender mod not resetting currency on a rejoin at all.\n\n(a) is the honest one and it is the one that needs agreement across the repos.\nTracked from the other side as mvm-6dc, which needs the same total for\nmid-mission class changes.","created_at":"2026-08-26T08:31:23Z"},{"id":"01a03d95-076e-7cd7-86b6-56e17138e88b","issue_id":"apw-4cb","author":"m-this","text":"Option (a) is done, both sides, and the rejoin no longer drops bundles.\n\nThis plugin publishes TF2AP_GetBundleCredits(client) from AskPluginLoad2 and\nregisters the library name tf2_archipelago. It returns MvM_BundleCredits, the\nper-client ledger. A native rather than a convar or a file: the bot mod reads it\nat the moment a bot spawns and needs the value now, not one from whenever\nsomething last wrote it down.\n\nThe defender mod (tf2-mvm-bots 97b4bfd) declares it optional and routes its\njoin-time write through one helper:\n\n    SetCurrencyWithBundles(client, earned)\n      -\u003e TF2_SetCurrency(client, earned + GetArchipelagoCredits(client))\n\nOne place that knows Archipelago exists, so a bare TF2_SetCurrency is now the\nthing that looks wrong in a diff. The original bug was one forgotten term in one\nexpression and this is the shape that stops a second call site forgetting it.\n\nTwo things that were nearly wrong, kept here because they are the parts that\nwould rot quietly.\n\nMarkNativeAsOptional is load-bearing and does not look it. An unresolved native\nin SourcePawn does not fail at the call, it fails the whole plugin load, so\nwithout that line every server that has never heard of Archipelago would get no\ndefender bots at all. The GetFeatureStatus guard next to it does not help: it\nonly ever runs on a plugin that already loaded. They look like the same check\nand they are not.\n\nThe lookup is redone on OnLibraryAdded, OnLibraryRemoved and OnAllPluginsLoaded\nrather than cached at startup, because this plugin can be reloaded while the mod\nruns and a stale \"not available\" would last until the map changed.\n\nVerified on the test-bed both ways: with the plugin staged, eight plugins and\nboth load; without it, seven and Defender TFBots 1.5.5 still loads, no failures.\n\nWhat is still open here is the refund and wave-loss accounting this bead started\non, which is unaffected. The ledger it keeps is now read by two plugins instead\nof one.","created_at":"2026-08-26T10:19:32Z"}],"dependency_count":0,"dependent_count":1,"comment_count":4}
{"_type":"issue","id":"apw-5nv","title":"Saving a loadout does not reach the menus that assign it","description":"Reported: 'when creating a loadout, saving, it needs the whole settings to be\nsaved even if i just saved a new loadout to be able to assign it to a bot'.\n\nThe Windows settings window builds every loadout menu on the Team and Classes\npages once, from the library as it stood when the dialog opened. A walk ComboBox\nkeeps the model it was built with, so saving on the Loadouts page changed the\ndata and nothing on screen. Assigning a new loadout to a bot meant saving every\nother setting, reopening, and coming back.\n\nThe TUI is not affected: save and remove both call build(), which makes the\nfields again from the edited settings.\n\nWanted: the menus refill on save and on remove, and a menu whose loadout was\nremoved falls back to that class's stock.","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T13:34:17Z","created_by":"m-this","updated_at":"2026-08-27T13:34:26Z","closed_at":"2026-08-27T13:34:26Z","close_reason":"Fixed. botTeamEditor.refreshLoadouts refills every seat and class loadout\nmenu, and the Loadouts page calls it after Save and after Remove.\n\nThe selection is put back by name rather than by index, because saving a\nloadout inserts it among the others and every index after it moves; and by name\nrather than by the whole label, because saving over a name changes the weapons\nthe label spells out and the menu should stay on the loadout the seat still\nnames. A removed loadout falls back to index zero, which is the class's stock.\n\nreselectLoadout carries that rule and lives in the non-Windows file, so three\ntests cover it: one asserts the new loadout really did shift the picked index\nbefore checking the pick survived, or it would prove nothing.\n\nAlso fixed while here: botTeamEditor.show resolved a saved team's loadouts with\nclass.LoadoutByKey, which knows nothing about custom ones, so loading a team\nthat named a custom loadout showed stock. It goes through the library now.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-pni","title":"A buff menu opens itself at the upgrade station and takes the number keys","description":"Reported 2026-08-27: \"the buff thing that shows on the left of the screen, wtf\nis that?\"\n\nIt is a SourceMod menu, which TF2 draws down the left, listing every weapon buff\non the current loadout. WeaponBuffs_ShowMenu, called from\nMvM_OnCommandKeyValues on MvM_UpgradesBegin: it opens itself every time the\nplayer opens the upgrade station, and stays for twenty seconds.\n\nCame in with the weapon buffs in PR #3.\n\n## Why it is worse than clutter\n\nA SourceMod menu takes the number keys while it is up. In MvM the number keys\nare weapon slots, so for twenty seconds at the station the player cannot switch\nweapons with them, and nothing on screen says why.\n\nIt also arrives unasked. Reading your buffs is a thing you want when you want\nit; the station is where you are already reading a different menu.\n\n## What it should probably be\n\nThe information is worth having, so this is about how it arrives:\n\n- on request, as a chat command beside !ap bots and !ap status, rather than\n  automatically\n- or as a panel or hint text, which draws without taking the keys\n- and if it stays automatic, it wants an obvious way out and a much shorter life\n  than twenty seconds\n\nWorth asking kelly-cs what he intended before changing it, since he built it and\nmay have had a reason for the station specifically.","status":"open","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T12:51:29Z","created_by":"m-this","updated_at":"2026-08-27T12:51:29Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-7w9","title":"A shield gives the Demoman a locked secondary back","description":"Discord, 2026-08-27, k-kaneta: \"Demo can still charge without secondary, if you\nuse shield\"\n\nThe run locks weapon slots until the multiworld hands them over. A Demoman whose\nsecondary is still locked can equip a shield and charge with it, so the lock is\nbypassed and the check it was gating is worth nothing.\n\n## The likely mechanism\n\nUnlocks_Strip walks the slots and calls TF2_RemoveWeaponSlot for each one that\nis not open (unlocks.inc:371). That removes a weapon.\n\nA shield is not a weapon. The Chargin' Targe and its kin are tf_wearable_demoshield\nentities: they sit in the secondary slot of the loadout and never appear in\nGetPlayerWeaponSlot, so the strip walks past them. The charge comes off the\nshield's own attributes, which are still on the player.\n\n## What else this would cover\n\nAny other wearable that occupies a slot the same way. The Sniper's Razorback and\nDarwin's Danger Shield are the obvious pair, both secondary. Worth checking\nwhether they survive a locked secondary too, because the fix is the same one.\n\n## How to check it\n\nLock the secondary, spawn a Demoman with a shield, and see whether the shield is\nstill worn and the charge still available. The strip already logs; it just has\nnothing to say about an entity it never looked at.","status":"open","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T11:24:20Z","created_by":"m-this","updated_at":"2026-08-27T11:24:20Z","comments":[{"id":"01a0430f-0a20-76a2-8416-13c838554ffd","issue_id":"apw-7w9","author":"m-this","text":"The same hole, with the opposite symptom. k-kaneta, minutes after the shield\nreport:\n\n  \"wonder what happens with boots\"\n  \"lmao he holds nothing\"\n\nAli Baba's Wee Booties and the Bootlegger occupy the Demoman's primary slot and\nare tf_wearable, not weapons. So with the primary locked, TF2_RemoveWeaponSlot\nfinds no weapon to take and the boots stay on: he has no primary in hand and is\nleft holding nothing.\n\nTwo symptoms, one cause. A wearable in a locked slot is invisible to the strip,\nand depending on the item that either hands back an ability the lock was meant\nto take (the shield's charge) or leaves the player with no weapon at all (the\nboots).\n\nSo the fix has to remove the wearable as well as the weapon, and then make sure\nsomething is in his hands afterwards. SwitchToAnyWeapon already exists for the\ncase where the strip removed what he was holding.","created_at":"2026-08-27T11:50:54Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"apw-8t6","title":"Nobody can tell whether the server needs our seed or theirs","description":"Discord, 2026-08-27, Red (xLander), who could not work it out from the docs:\n\n  \"what's the deal with make seed? Is there a reason the multiworld is generated\n   by the makefile here rather than the standard archipelago generation?\"\n  \"I can just throw a yaml in with our multiworld, generate, and then change the\n   environment config for the server to that slot and server and connect and\n   it'll work?\"\n  \"I just wasn't seeing the utility in having your thing generate a multiworld\n   as opposed to doing it the normal way\"\n\nomaewh4: \"I added the seed generation because I thought that would save a step\nfor people to setup their game, it might be confusing though. Is the wiki\nlacking for docker deployment part?\"\n\n## The actual question, which the docs never answer\n\nIs the server seed-agnostic? Somebody joining an existing multiworld does not\nwant to generate anything: they want to point the stack at a room and a slot.\nmake seed is a convenience for people starting from nothing, and it reads as a\nrequirement.\n\nThat is a first-contact problem. Somebody who already runs Archipelago is the\nmost likely person to try this, and they are the ones it confuses.\n\n## What to write\n\nSay it in the first paragraph of the install pages, before make seed appears:\n\n- the stack plays a room that already exists, and AP_HOST, AP_PORT and\n  AP_SLOT_NAME are all it needs\n- make seed is one way to get a room, for people who have none, and nothing\n  more\n- generating the normal way and pointing the stack at the result is supported\n  and is the same thing\n\nThen check the Docker page answers it too, since that is where he was.","status":"open","priority":1,"issue_type":"task","owner":"m@this.fr","created_at":"2026-08-27T10:01:21Z","created_by":"m-this","updated_at":"2026-08-27T10:01:21Z","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-4zk","title":"Only Valve's six maps, and the community has hundreds","description":"What kelly-cs's PR #2 solves, alongside its prerequisite mvm-78m in\ntf2-mvm-bots.\n\n## The problem\n\nA run draws from Valve's own missions and nothing else, so every run visits the\nsame six maps. potato.TF and the rest have hundreds, and the run has no way to\nreach them.\n\nNothing bundles third-party assets, so the work is not \"ship more maps\": it is\nletting somebody fetch them, checking what they fetched is usable, and refusing\nwhat is not.\n\n## What the PR does\n\n- a validated community mission catalogue, with the assets fetched rather than\n  bundled, and a local ZIP import for people who already have them\n- Valve and community missions kept visibly apart in the launcher\n- maps with no bot .nav file shown in red and unselectable, because a map the\n  bots cannot walk is a mission nobody can win\n- checks the selected pool can satisfy the run before generation, rather than\n  finding out at the seed\n- installs maps, popfiles, nav meshes and custom upgrade files on Windows and\n  Linux\n- raises net_maxfilesize to 64 MB, since the default 16 breaks the downloads\n\n## Why the prerequisite exists\n\nOn some community maps the bots have a nav mesh and still never leave spawn.\nThat is mvm-78m, merged as tf2-mvm-bots#1, and without it a drawn community map\ncan be a mission the team stands in spawn for.\n\n## Worth knowing\n\nThe nav check is the part that keeps this honest: it refuses a map rather than\nletting a run draw one the bots cannot play. That is the same rule the weapon\ncatalogue follows, where a menu entry the mod cannot drive is worse than a\nmissing one.","status":"closed","priority":1,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-27T09:28:45Z","created_by":"m-this","updated_at":"2026-08-27T13:14:26Z","closed_at":"2026-08-27T13:14:26Z","close_reason":"Shipped. kelly-cs's PR #2 merged in 20479f6, CI and nightly green: community missions fetched rather than bundled, Valve and community kept apart in the list, maps without a bot nav file refused, and the pool checked against the run before generation.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-4kb","title":"Knobs for bot damage and health, so the balance can be measured","description":"Asked for directly: \"Balancing (damage, health, will see what works and what\ndoesn't)\".\n\nThe second half of that sentence is the whole design. Nobody knows yet what the\nright numbers are, so the deliverable is not a number: it is the pair of knobs\nplus a way to read what moving them did.\n\n## Why it is not just a convar\n\nValve tunes every wave for six defenders. The bots exist so fewer can win, and\nwhether they do is unmeasured. docs/testbed-metrics.md is the standing rule\nhere: a change to how the bots fight ships on only once a run has said it\nhelped.\n\nSo this bead is three things, in order:\n\n1. Two convars, `sm_redbots_manager_bot_damage_scale` and\n   `sm_redbots_manager_bot_health_scale`, both defaulting to 1.0 so the mod\n   plays exactly as it does today until somebody asks otherwise.\n2. Feature switches in source/redbots3/features.sp, so the two arms of an A/B\n   are the same build.\n3. An A/B on at least two maps, because mvm-15t exists precisely because a\n   one-map result was not trusted.\n\n## What to watch\n\nDeaths per wave and waves lost are the numbers that decide it, and\ntestbed/results/ holds what the current build does. A damage scale that wins\nwaves by making the bots do the players' job for them is not a win: the run has\nto stay a run.\n\n## Where it is exposed\n\nIf it ships, the launcher's Bots page gets the two, and the .env gets\n`SRCDS_BOT_DAMAGE_SCALE` and `SRCDS_BOT_HEALTH_SCALE` beside the team size,\nwhich is the knob that does this job crudely today.","status":"closed","priority":1,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-26T22:00:35Z","created_by":"m-this","updated_at":"2026-08-26T22:01:10Z","closed_at":"2026-08-26T22:01:10Z","close_reason":"Filed in the wrong database. It is mod work with a launcher half, so it lives in tf2-mvm-bots as mvm-bha.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-7zi","title":"A sentry buster blowing itself up counts as the giant kill","description":"Reported from a nightly run on Decoy. A sentry buster detonating on a sentry is\nannounced as \"Giant killed\" and sends the mission's giant check.\n\n## Why\n\n`MvM_IsGiant` in `plugin/scripting/tf2_archipelago/mvm.inc` asks one question:\nis this a BLU fake client with `m_bIsMiniBoss` set. A sentry buster carries that\nproperty, so it answers yes. `Event_PlayerDeath` in `tf2_archipelago.sp:345`\nfires on the buster's death like any other, and the check goes out.\n\nThe detonation is a death like any other as far as the event is concerned. The\nbuster does not even have to be killed by anybody.\n\n## The part that is worse than a wrong announcement\n\n`g_GiantReported` latches. `Event_PlayerDeath` returns immediately once it is\nset, and it is only cleared at wave 1 of a mission. So the buster does not just\nsend a wrong check, it **spends the mission's only giant check**. The real giant\nthat dies later in the run reports nothing, because the flag is already true.\n\nThat makes this quieter than it looks. On any mission with a buster before the\nfirst giant, the giant objective is decided by a robot blowing itself up, and\nnothing in the log says the real one was missed.\n\n## Fix\n\nThe sibling repo already solved the detection, and it is one string compare.\n`tf2-mvm-bots`, `source/redbots3/util.sp:534`:\n\n    bool IsSentryBusterRobot(int client)\n    // ... GetClientModel, then\n    // StrEqual(model, \"models/bots/demo/bot_sentry_buster.mdl\")\n\nAdd the same test to `MvM_IsGiant` and return false for it. Same approach, same\nmodel path, already proven against live servers by the bot mod, and it needs no\nnew gamedata.\n\n## One decision to make first\n\nWhether a buster counts when players **kill** it rather than letting it\ndetonate. The report is about self-detonation, but a buster is not a giant\neither way, and the simpler rule is that a buster is never a giant. The\nalternative, excluding it only when it kills itself, needs the `player_death`\nattacker compared with the victim and leaves the odd case where shooting one\ncounts and letting it go does not.\n\nRecommend excluding the buster outright. It is one rule rather than two, and it\nmatches what a player means by \"we killed a giant\".\n\n## Test\n\nThe testbed can drive this: spawn a buster, let it detonate, and confirm no\n`giant_killed` objective reaches the bridge and that `g_GiantReported` stays\nfalse so a later giant still reports.","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T15:29:42Z","created_by":"m-this","updated_at":"2026-08-26T15:37:03Z","closed_at":"2026-08-26T15:37:03Z","close_reason":"Fixed. MvM_IsGiant now returns false for the sentry buster model, using the same test the defender bot mod already ships in source/redbots3/util.sp. Excluded outright rather than only on self-detonation, which is one rule instead of two. Plugin compiles clean.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-aga","title":"The grant watchdog fights the deliberate wait, and restarts the poll every five seconds","description":"Not reported by anybody. Found in k-kaneta's second bundle while chasing the\ncrash. `launcher.log` holds this line 282 times in one session:\n\n    [AP] debug: The grant poll had stopped. The plugin starts it again.\n\nEvery five seconds, from the first grant to the end of the session.\n\n## What starts it\n\nThe first one lands right after the first held grant:\n\n    20:28:48  launcher test mode: Bram sent us something\n    20:28:48  srcds  [AP] 200 credits are waiting for the upgrade station.\n    20:28:48  srcds  [AP] debug: The grant poll had stopped. The plugin starts it again.\n\n\"Waiting for the upgrade station\" is the hold. `OnGrants` handles it at\n`plugin/scripting/tf2_archipelago/bridge.inc:687`, and the comment says exactly\nwhat it means to do:\n\n\u003e A held effect leaves the cursor behind the bridge, so the next poll answers\n\u003e at once with the same grant. Asking straight away would be a loop as fast as\n\u003e the loopback; asking on the retry timer is what turns the hold into a wait\n\u003e for the upgrade station.\n\nSo it calls `Bridge_PollGrantsLater()` and returns, leaving `g_PollingGrants`\nfalse on purpose while the retry timer runs.\n\n`Timer_DrainPending` at line 273 then sees exactly that:\n\n    if (g_HaveUnlocks \u0026\u0026 !g_PollingGrants)\n\nand cannot tell a loop that died from a loop that is deliberately waiting. It\nlogs, and calls `Bridge_PollGrants()`.\n\n## Why it never settles\n\nBoth timers use the same constant:\n\n    #define RetryInterval 5.0\n\nThe watchdog is `CreateTimer(RetryInterval, Timer_DrainPending, _, TIMER_REPEAT)`\nat line 106. The hold's retry is `CreateTimer(RetryInterval, Timer_PollAgain)`\nat line 639. Identical intervals, so the watchdog lands in the same window as\nthe pending retry every single time. It is not a rare race, it is the normal\ncase, which is why the count is 282 and not 3.\n\n## What it costs\n\nThree things, in rising order of seriousness.\n\n1. The debug log. 282 lines of a message that means \"something is wrong\" when\n   nothing is. A debug bundle is only worth what it holds, and this drowns it.\n2. Two live schedules for one loop. The watchdog's poll and the pending\n   `Timer_PollAgain` both fire, so grant requests overlap where the design says\n   one connection sits open.\n3. The hold stops holding. The whole point of `Bridge_PollGrantsLater` is to\n   wait for the upgrade station instead of spinning on the loopback. The\n   watchdog re-asks anyway, so the wait it buys is gone.\n\n## Fix\n\nThe watchdog needs to distinguish \"no request in flight and none scheduled\"\nfrom \"no request in flight\". Track the scheduled retry — a `g_GrantRetryDue`\nflag set in `Bridge_PollGrantsLater` and cleared in `Timer_PollAgain` — and\nhave `Timer_DrainPending` skip when it is set.\n\nThe same shape applies to the other three loops the watchdog covers.\n`Bridge_PollMessagesAgain` (line 830) and `Bridge_PollDeathsAgain` (line 887)\nschedule retries on the same constant against the same `!g_PollingChat` and\n`!g_PollingDeaths` tests, so they can misfire the same way. They do not show up\nhere only because nothing held them during this session. Fix all four together\nrather than patching the one the log caught.\n\nKeep the watchdog. Its comment explains why it exists — a native error in a\ncallback ends a self-sustaining loop with no timer left to notice — and that\nreasoning is still right. It just has to stop counting a planned wait as a\ndeath.","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T13:18:26Z","created_by":"m-this","updated_at":"2026-08-26T17:05:29Z","started_at":"2026-08-26T17:04:32Z","closed_at":"2026-08-26T17:05:29Z","close_reason":"Fixed. The four loops record a retry deadline and the watchdog skips a loop with one booked, plus a one-second grace so it does not race a retry landing on the same poll. A deadline rather than a flag keeps the watchdog's guarantee: a retry that never arrives falls back to it. Chat, deaths and unlocks were fixed alongside grants; they had the same shape and only escaped because nothing held them in the bundle.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-ygn","title":"A blacklisted class still gets drawn into a partial lineup","description":"Cowser, on native Linux, running 1.9.0:\n\n\u003e i also have spy blacklisted for bots but nope\n\nHis screenshot shows the RED lineup with a Spy in it. The seven names on it —\nCowser, The Combine, DeadHead, Kill Me, MindlessElectrons, Grim Bloody Fable,\nLOS LOS LOS — are exactly the seven upgrade buyers in the `console.log` of his\nbundle, so the screenshot and the logs are the same session. (It is also the\nsame session as apw-49k, which is why there are seven of them.)\n\nBot class is never printed to the log, so the logs neither confirm nor deny\nthis. The screenshot is the evidence.\n\n## Not the checkboxes\n\nRuled out. His `config.json` holds `\"srcds_bot_class_blacklist\": [\"spy\"]`, so\nthe TUI toggle recorded the change correctly. Whatever went wrong is\ndownstream of the widget.\n\n## Two causes, and both are real\n\n**apw-2sw is the first**, split out because it is much larger than this bead:\nthe TUI never calls `srcdsconfig.Install`, so his `server.cfg` was rendered\nbefore he unticked the Spy and never rewritten. That server ran with\n`sm_redbots_manager_class_blacklist \"\"` for the whole session. Fix that and\nthis session stops reproducing.\n\n**This bead is the second**, and it survives the fix. Live in current code,\nnot just 1.9.0. His settings are `srcds_bot_team_comp: [\"scout\"]` and\n`srcds_bot_class_blacklist: [\"spy\"]`:\n\n    botloadout.Composition([]string{\"scout\"}, []string{\"spy\"})  =\u003e  \"scout\"\n\nOne named seat, six to fill. Our own generated `server.cfg` states the rule\nnext to the convar: \"A team named here beats the blacklist above.\" So the mod\ndraws the other five itself, and nothing is stopping it drawing a Spy. A\ncorrectly rendered blacklist would not have helped, because the lineup outranks\nit.\n\n`Composition` already knows this matters. When the lineup is empty and the\nblacklist is not, it pads out to `botSeats` so that a team of nothing but draws\nstill writes its seats — the comment says as much. The gap is that the padding\nonly happens when `seats` is empty. A lineup with one entry in it skips the\npad and hands five seats back to the mod.\n\n## Fix\n\nPad to the team size whenever the blacklist is non-empty, not only when the\nlineup is empty. The trailing-empty trim above it must not then undo the pad.\n`Composition([\"scout\"], [\"spy\"])` should render six seats, not one.\n\nCheck the padding actually does what the empty-lineup case assumes — that a\nwritten empty seat respects the blacklist while an unwritten one does not. If\nthat is not true, then the blacklist cannot be honoured through the lineup at\nall, and it has to be enforced on our side after the mod picks.\n\n## Test\n\nA table test on `Composition` covers the fix. Reproducing the original needs\napw-2sw fixed first, or the blacklist never reaches the server to be beaten.","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T13:03:48Z","created_by":"m-this","updated_at":"2026-08-26T17:04:20Z","started_at":"2026-08-26T17:04:19Z","closed_at":"2026-08-26T17:04:20Z","close_reason":"Fixed. Composition now pads to the team size whenever the blacklist is non-empty, not only when the lineup is empty, so no seat is left for the mod to draw without consulting it. Three new tests cover the short lineup, the no-blacklist case, and a lineup longer than the settings page offers. Two existing expectations changed because they encoded the old behaviour on blacklisted input.","dependencies":[{"issue_id":"apw-ygn","depends_on_id":"apw-2sw","type":"blocks","created_at":"2026-08-26T14:09:02Z","created_by":"m-this","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-17x","title":"[bug] The bridge stops and drops the room, repeatedly","description":"k-kaneta, playing 1.9.0:\n\n\u003e and \"bridge stopping\" then disconnecting is still common issue to me\n\n\"Still\" and \"common\", so this is neither new nor rare, and it has never been\nfiled with a cause attached.\n\n## No evidence yet, and that is the first job\n\nCowser's debug bundle was collected the same day and does not show this: his\n`bridge.json` reads `connected: true` with an empty `last_error`, and his\nconsole log has no bridge stop in it. Different player, different machine, so\nhis bundle cannot settle it either way.\n\nAsk k-kaneta for a debug bundle taken after it happens. Specifically wanted:\n\n- `bridge.json`, for `last_error` and `last_check` at the time of the drop\n- the launcher log around the \"bridge stopping\" line\n- whether the room is `archipelago.gg` hosted or self-hosted, and whether the\n  TF2 server keeps running when the bridge goes\n\n## What to look at meanwhile\n\n\"Bridge stopping\" is our own message, so find every place that emits it and\nwork out which one fires. The distinction that matters: the bridge deciding to\nstop, versus the bridge dying and the supervisor reporting it. Those are\ndifferent bugs and the current message does not tell them apart.\n\nMake the message say which. Whatever the cause turns out to be, \"bridge\nstopping\" with no reason attached is why this report has gone unfixed long\nenough to be described as common.\n\nThen check the reconnect path. ADR 0002 argues for a long-lived bridge\nprecisely so a dropped room comes back on its own. If a stop means the player\nhas to restart, the behaviour the ADR specifies is not what ships.","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T12:30:12Z","created_by":"m-this","updated_at":"2026-08-26T13:18:33Z","closed_at":"2026-08-26T13:18:33Z","close_reason":"Superseded by apw-r85. k-kaneta's second bundle shows the launcher log, and it inverts this bead's premise: the game server crashes with 0xc0000005 and the launcher's own shutdown prints 'bridge stopping' on the way out. The bridge is not the failing component. Everything asked for here is now in apw-r85 with the evidence attached.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-49k","title":"RED overfills to seven, and the returning player is sent to spectator","description":"Cowser, on native Linux, playing 1.9.0 with bot fill set to 6:\n\n\u003e oh fuck the first one has 7 players\n\u003e ok what is with the 7 players\n\nHis debug bundle carries the proof, and the cause is in our code. His\nscreenshot of the RED lineup shows the seven slots directly.\n\n## What the log shows\n\nAt 07:58:47 the fill decision is right:\n\n    RED holds 1 of 6 between waves. 5 bot(s) take the empty seats.\n\ntaken=1 is Cowser, RED holds no bots, so it asks for 5. Then seven distinct\nnames buy upgrades in that same session: Cowser plus The Combine, Kill Me,\nDeadHead, Grim Bloody Fable, MindlessElectrons and LOS LOS LOS. Six bots, not\nfive. `sm_addbots 5` added six.\n\nThose are the same seven names as on the screenshot, so the screenshot, the\nlog and apw-ygn are all one session.\n\nThe previous session in the same bundle added five and stayed at six total, so\nthe overshoot is not every time.\n\nThen, at the end of that session:\n\n    MVM assigned Cowser the Khelinace to spectator, all slots for defending\n    team are in use, or reserved for lobby members\n\nThat is the second half of the complaint. RED was full of bots, so the human\ncould not get back on it.\n\n## Cause\n\n`Bots_TrimSurplus` already exists and already knows about the overshoot; its\nown comment says \"one ask can add more than it asked for\". The problem is when\nit runs. `plugin/scripting/tf2_archipelago/bots.inc:490` calls it at the *top*\nof `Bots_Fill`, before the `sm_addbots` on line 528. So a poll trims the\nprevious poll's surplus, never its own.\n\nThat would self-correct on the next poll, except `Bots_Fill` returns early at\nline 477 unless the round state is `RoundState_BetweenRounds`. Once the wave\nstarts, the poll returns before it reaches the trim, and the surplus bot rides\nout the whole wave in a seat that belongs to a player.\n\n## Fix\n\nTrim after the add, not only before it. `Timer_TrimBots` is already a one-shot\ntimer built for exactly this, and it is currently only ever fired from inside\n`Bots_Fill`. Arm it after the `sm_addbots` so the count settles once the mod\nhas taken its turn.\n\nThen make the trim reachable during a wave. Either drop the `BetweenRounds`\nguard for the trim path, or fire `Timer_TrimBots` on the round-start hook. A\nsurplus bot found mid-wave should still leave, since the seat it holds is what\nsends the player to spectator.\n\nNote the two counts disagree on spectators, and that is deliberate on the fill\nside: `Bots_Fill` counts any connected human as holding a seat, while\n`Bots_TrimSurplus` counts RED only. Keep the fill rule. Make sure the trim\nnever picks a seat the fill is holding for a human, or the two will fight.\n\n## Test\n\nThe testbed can drive this: fill to 6, confirm the count settles at 6 and not\n7, then disconnect and reconnect the human mid-mission and confirm they land on\nRED rather than spectator.","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T12:28:49Z","created_by":"m-this","updated_at":"2026-08-26T17:07:11Z","closed_at":"2026-08-26T17:07:11Z","close_reason":"Fixed. Timer_TrimBots is armed after sm_addbots as well as before it, so a poll trims its own overshoot instead of leaving it for a poll that returns early once the wave starts. Bots_TrimSurplus now uses the same seat arithmetic as Bots_Fill via Bots_SeatsHeldOffRed, so RED stops being held full of bots while a reconnecting player sits in spectator. Plugin compiles clean. Not reproduced end to end: the testbed check in the bead (fill to 6, disconnect, reconnect mid-mission) is still worth running.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-6xe","title":"The Docker install downloads a .env.example that 404s","description":"The Docker quick start in both languages tells a player to run\n\n    base=https://github.com/m-this/tf2-archipelago/releases/latest/download\n    curl -fsSL -o .env \"$base/.env.example\"\n\nand that URL returns 404. Checked against v1.9.0 today: .env.example is 404,\ndefault.env.example is 200.\n\nThe cause is the upload, not the docs. `gh release create` renames a file whose\nname starts with a dot, so deploy/.env.example lands on the release as\ndefault.env.example. It has been that way on every release, and it is on the\nnightly prerelease too, because both jobs upload the same asset list.\n\nNote curl -fsSL is silent on failure by design, so the player gets no error and\nno .env, then hits SRCDS_RCONPW being unset several steps later with nothing\npointing back here.\n\nTwo ways to fix it and they are not equal.\n\nRename the asset so the documented name is the real one: copy it to\ndist/env.example in `make dist` and upload that, then say curl -o .env\n\"$base/env.example\". No leading dot, nothing to rename, and the same URL works\non a release and on nightly. The docs change with it.\n\nOr leave the upload alone and fix four documents to say default.env.example.\nCheaper now and it keeps a name that reads like an accident, which somebody will\ntry to correct later.\n\nThe first one. Whichever is chosen, the files to change are\ndocs/en/setup/install.md, docs/fr/setup/install.md and the index page in both\nlanguages, and the fix wants checking with a real curl rather than by reading.\n","status":"closed","priority":1,"issue_type":"bug","assignee":"m-this","owner":"m@this.fr","created_at":"2026-08-26T11:33:46Z","created_by":"m-this","updated_at":"2026-08-26T17:06:53Z","started_at":"2026-08-26T17:05:50Z","closed_at":"2026-08-26T17:06:53Z","close_reason":"Fixed by renaming the asset, which was the recommended option: dist carries env.example with no leading dot, both workflows upload that name, and the two install pages plus their surrounding prose fetch it. Not verified against a live release URL, because GitHub Actions is in a major outage and no release or nightly has completed since the change. The 200/404 check should be redone once one does.","labels":["docs"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-4i1","title":"Bots have never been paid a Cash Bundle","description":"Every Cash Bundle the run receives goes to the humans only. The bot team, which\nin most runs is five of the six seats, has never seen a credit of Archipelago\nmoney.\n\nMvM_GrantCredits in mvm.inc walks the clients and pays each one that passes\nMvM_IsPlayer, and MvM_IsPlayer is:\n\n    client \u003e 0 \u0026\u0026 client \u003c= MaxClients\n        \u0026\u0026 IsClientInGame(client)\n        \u0026\u0026 !IsFakeClient(client)\n        \u0026\u0026 GetClientTeam(client) == TeamRed\n\nThe !IsFakeClient is doing the excluding. It is right everywhere else it is\nused, so do not touch it: unlocks.inc leans on it to keep class and slot\nenforcement off the robots, and tf2_archipelago.sp:459 says so outright, \"Both\nevents fire for robots too; MvM_IsPlayer is what leaves their loadout alone\".\nThe payout needs its own predicate for a RED defender, human or bot, and\nMvM_CanPayCredits should keep asking about humans, since it is asking whether\nthe moment is payable rather than who is owed.\n\n## Why it matters more than the credits\n\nA bundle is 200 credits (cashBundleCredits, gamedata/items.go). Filler-heavy\nspheres hand out a lot of them, and the player watches their own balance climb\nwhile the team that has to actually hold the bomb stays poor. That reads as the\nbots being bad at upgrading, which is the complaint that keeps coming in, when\npart of it is that they were never given the money.\n\n## What to watch out for\n\nThe pack path is deliberate and the docblock on MvM_GrantCredits explains why:\nwriting m_nCurrency puts a number on the player the game's bookkeeping never\nsaw, and the refund button and a lost wave both restore that record, which is\nwhere the negative balances came from. So bots get a pack at their feet like\nanybody else, not a property write.\n\nThat means it has to be verified that a bot actually walks over it. It is\ndropped at the client's own origin between waves, which should be a touch on the\nspot, but a bot frozen in spawn is a known state on the mvm-bots side and a bot\nthat is not standing where the pack landed is not paid. Count what was paid,\ndo not assume it.\n\nThe other half is the defender mod's own accounting. It sets a bot's currency\nwholesale on spawn:\n\n    TF2_SetCurrency(data, GetStartingCurrency(g_iPopulationManager)\n                          + GetAcquiredCreditsOfAllWaves())\n\nNeither term knows about bundles, so a bot that is paid and then respawns loses\nthe money again. Fixing the payout without fixing that formula gets the money in\nand drops it on the next respawn. Tracked from the other side as mvm-6dc, which\nneeds the same running total of bundles granted.\n\n## Done when\n\nA run with several bundles granted shows each bot's balance matching the human's\nto the credit, across a wave loss, a refund, and a respawn.\n","status":"closed","priority":1,"issue_type":"bug","assignee":"m-this","owner":"m@this.fr","created_at":"2026-08-26T06:59:31Z","created_by":"m-this","updated_at":"2026-08-26T07:16:27Z","closed_at":"2026-08-26T07:16:27Z","close_reason":"Duplicate of apw-4cb, which is further along and already rules out the pack route this bead assumed. The half it had that apw-4cb did not, bots being excluded from the payout by MvM_IsPlayer, is folded in as a comment there.","labels":["discord"],"dependencies":[{"issue_id":"apw-4i1","depends_on_id":"apw-4cb","type":"related","created_at":"2026-08-26T08:16:32Z","created_by":"m-this","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-ys5","title":"Join from the launcher fails where the LAN browser succeeds","description":"The Blabber: \"just getting the 'connection failed after 4 retries.' message, and\nit stalls with like 2 bars\" / \"it tries to make me join\" / \"the server IS up, i\ndo start joining it\".\n\nPeppy walked him to the workaround: check the LAN tab in the server browser.\nIt was listed there, and connecting through it worked first try.\n\nSo the server is up and reachable; it is the launcher's own connect that fails.\nThe retry message and the two-bar stall are the symptoms to reproduce against.","design":"## Cause, from the evidence rather than a repro\n\n`SteamConnectURL` built the link from `LocalAddresses()[0]`, and that was\nwhatever `net.Interfaces` returned first. The order is arbitrary and a machine\nwith Docker, WSL or a virtual machine on it has several addresses that answer\nnothing.\n\nCowser's bundle carries four for one server:\n\n    192.168.50.105   192.168.34.1   192.168.222.1   172.25.192.1\n\nThree of those are adapters with no server behind them. A join aimed at one is\n\"connection failed after 4 retries\" and a stall at two bars, while the LAN tab\nof the server browser finds the server first try because it looks on the\nnetwork rather than at a list. That is The Blabber's report line for line.\n\nThe old comment already admitted the choice was arbitrary: \"the link uses the\nfirst address the machine has\".\n\n## Fix\n\n`preferredOutboundAddress` dials UDP at 192.0.2.1, the documentation range.\nNothing is sent and nothing routes there: the dial only makes the kernel choose\na route, and the local address of that choice is the interface this machine\nreally reaches the network on. `LocalAddresses` puts it first, so the join link\nand the displayed list both lead with it.\n\n## Not reproduced\n\nThe Blabber's machine is not available and the fault needs several adapters to\nshow. The evidence is his symptoms matching this mechanism exactly, plus a\nsecond bundle showing the multi-adapter case is real and common. Worth asking\nhim to try a build past this before the bead is closed.\n\nIf it still fails for him, the next thing to rule out is the password: the link\ncarries `+password` only when `SrcdsPw` is set, and a server with one set after\nthe link was built would refuse a join that looks identical from outside.","status":"open","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T14:12:10Z","created_by":"m-this","updated_at":"2026-08-26T18:49:33Z","started_at":"2026-08-26T18:47:56Z","labels":["discord"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-48u","title":"De-upgrading appears not to take","description":"From the play-test: buy an upgrade, sell it again before leaving the upgrade\nstation, and it stays bought. Reported as \"not sure if it is the AP or MvM in\ngeneral\", and that is still the open question.\n\nNothing in this plugin touches a purchase. `Unlocks_EnforceSlots` removes\nweapons in slots the run has not opened and leaves every other slot alone, and\nthe upgrade command is only read, never blocked.\n\nThe hypothesis worth testing first is item 1 above. `MvM_GrantCredits` writes\n`m_nCurrency` straight onto the player, so after a Cash Bundle the player's\ncredits and the number the game recorded for the wave disagree. A refund pays\nback through the game's own accounting, and accounting that has been written\naround from outside is exactly the kind that refuses a transaction while\nshowing it as accepted.\n\nThat gives a test that costs one evening: sell an upgrade on a run where no\nCash Bundle has landed yet, and sell one after a bundle has. If only the second\nfails, this is item 1 and not a Valve bug, and fixing item 1 fixes both.\n\nThe plugin now writes every purchase and every sale to the SourceMod log,\nplayers included, with the credits held afterwards. So a bundle from the\nevening says whether the sale reached the game at all, which is the half nobody\ncould see: a sale the station accepted and the game ignored looks the same as\none that worked, from the chair.","design":"## Prior art, and it points where the probe already pointed\n\nSource SDK 2013 carries the upgrade code officially. Read it, never copy it:\nits licence requires derivatives stay free and ship Valve's LICENSE, and this\nrepo is MIT, so the two cannot be mixed.\n\nTwo files matter, and which one matters is the whole question here:\n\n- `src/game/shared/tf/tf_upgrades_shared.cpp` holds the shared `MVM_Upgrade`\n  handling, the same message the probe drives with\n  `FakeClientCommandKeyValues`.\n- `src/game/client/tf/player_vs_environment/c_tf_upgrades.cpp` is the **client**\n  upgrade panel.\n\nThe probe ran ten conditions and every one came back `credits_back: true` and\n`upgrade_removed: true`, so the server takes the sale and returns the money.\nThe conclusion recorded here was that the leading remaining explanation is a\nstale station panel rather than a server bug. That is a client-side claim, and\n`c_tf_upgrades.cpp` is where it can be settled: how the panel holds its own copy\nof the upgrade state, and what makes it refresh.\n\nRead it for that question specifically. If the panel keeps a local tally and\nonly refreshes on certain events, a player who sells and sees no change is\nlooking at a stale panel over a server that already did the work, and no amount\nof server-side testing will ever reproduce it.\n\nNote there is no `RefundUpgrade` symbol in the public SDK, so the server-side\nrefund path may not be there in full. The client panel is the part that is, and\nit is the part this bead now needs.","notes":"Imported from TODO.md item 5 — section: Bugs, most impactful first\nOriginal heading: De-upgrading appears not to take. Open, and it may be item 1","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T13:52:59Z","created_by":"m-this","updated_at":"2026-08-26T21:19:06Z","closed_at":"2026-08-26T21:19:06Z","close_reason":"Closed","labels":["todo-import"],"dependencies":[{"issue_id":"apw-48u","depends_on_id":"apw-4cb","type":"blocks","created_at":"2026-08-26T09:33:23Z","created_by":"m-this","metadata":"{}"}],"comments":[{"id":"01a03d33-dfdd-7130-9533-8cce86f3b624","issue_id":"apw-48u","author":"m-this","text":"Marked blocked rather than open, because nothing here can move without a play\nsession that this bead cannot produce for itself.\n\nThe test it names costs one evening and no code: sell an upgrade on a run where\nno Cash Bundle has landed, then sell one after a bundle has. If only the second\nfails, this is apw-4cb and not a Valve bug. The purchase and sale logging is\nalready in, so a debug bundle from that evening answers it.\n\nBlocked on apw-4cb for the other reason too: if the hypothesis holds, fixing\napw-4cb fixes this and there is nothing separate to do.","created_at":"2026-08-26T08:33:25Z"},{"id":"01a03d43-12dd-7f40-a171-15201cf8f657","issue_id":"apw-48u","author":"m-this","text":"Unblocked, and the testbed can answer this without a play session. A probe is in\ntf2-mvm-bots as testbed/stats/mvmbots_refund.sp: it picks a live defender bot,\noptionally writes credits onto m_nCurrency the way MvM_DropCredits pays a\nbundle, buys an upgrade and sells it straight back, and prints spent, returned\nand sale_took as one line.\n\nA bot is a fair subject because the defender mod buys through\nFakeClientCommandKeyValues(\"MVM_Upgrade\") (upgrade.sp:1170), which is the same\ncommand the upgrade station sends for a human.\n\nDriving it turned up something that changes what this bead is asking, and it is\nworth writing down before the run finishes.\n\nEvery credit a defender bot has is written the same way a Cash Bundle is. The\nmod gives bots money with TF2_SetCurrency, and that stock is\n\n    SetEntProp(client, Prop_Send, \"m_nCurrency\", nAmount)\n\nwhich is byte for byte what MvM_DropCredits does. So a bot has never held a\ncredit the game's own record of the wave knew about, and bots buy upgrades\nsuccessfully all day: the mod checks that the credits actually moved\n(upgrade.sp:1157) and that check passes in play.\n\nThat is evidence against the broad version of the hypothesis. Money written from\noutside is spendable; the game does not refuse a purchase for it. What is left\nto test is narrower and more interesting: whether a *sale* specifically consults\nthe wave record that the write bypassed, which is exactly the asymmetry apw-4cb\ndescribes for the refund button.\n\nIt also means a bot cannot be the control leg. There is no game-recorded money\non a bot to compare against. The control needs either a human, or the test-bed\nhost, which joins normally and holds the starting 400 the game issued it.\n\nTwo practical notes for whoever picks this up. The host is a statue that never\nwalks to the upgrade station, and MvM validates that a buyer is standing in it,\nwhich is the likely reason the host accepted no upgrade at 400 credits. And\nBOT_USE_UPGRADES=0 keeps a bot's money unspent but also keeps it away from the\nstation, so it is not the shortcut it looks like.","created_at":"2026-08-26T08:50:01Z"},{"id":"01a03d56-9b04-7812-8583-a0b304377027","issue_id":"apw-48u","author":"m-this","text":"Tested on the bed. The hypothesis does not survive, and the bead needs a\ndifferent question.\n\nTwo legs, same subject, same upgrade, same cost. The test-bed host holds money\nthe game issued it, and the probe writes a bundle on top the way MvM_DropCredits\ndoes:\n\n  {\"bundle\":0,  \"started\":400,\"held\":400,\"spent\":150,\"after_sell\":400,\"returned\":150,\"sale_took\":true}\n  {\"bundle\":200,\"started\":400,\"held\":600,\"spent\":150,\"after_sell\":600,\"returned\":150,\"sale_took\":true}\n\nThe credits come back in full either way. Money written onto m_nCurrency from\noutside is not what stops a sale, so this is not apw-4cb and the two should stop\nbeing linked as cause and effect.\n\nWhat this does NOT show, and it is the half the report is actually about.\n\n\"De-upgrading appears not to take\" says the upgrade stays bought. The probe\nmeasures credits and only credits. A sale that hands the money back and leaves\nthe attribute on the player would pass every assertion above and still be the\nbug that was reported, and that combination is more consistent with the report\nthan a refused sale is: somebody who got no money back would have said the sale\ndid nothing, not that the upgrade stayed.\n\nSo the next measurement is the attribute, not the balance. Read the upgrade's\nlevel on the player or the weapon before the buy, after the buy and after the\nsale, and the answer is in the third number. tf2attributes is already staged on\nthe bed for it.\n\nMethod, for whoever picks it up. testbed/stats/mvmbots_refund.sp, run between\nwaves as `mvmbots_refund_probe \u003ccredits\u003e [host|bot]`, results in\naddons/sourcemod/logs/mvmbots_refund.jsonl. Use the host, not a bot: every\ncredit a bot holds was written with TF2_SetCurrency, which is the same\nm_nCurrency write, so a bot has no game-issued money to serve as a control.","created_at":"2026-08-26T09:11:21Z"},{"id":"01a03d6e-7aa4-7897-929a-f58de3500aae","issue_id":"apw-48u","author":"m-this","text":"Does not reproduce. Ten conditions on the test-bed, every one of them a clean\nsale: the credits came back in full and the attribute came off.\n\n  slot -1  upgrade 55  attr  60  0.750 -\u003e 0.000   bare mod, and again with the plugin\n  slot  0  upgrade  2  attr   6  0.899 -\u003e 0.000\n  slot  1  upgrade 19  attr  10  1.250 -\u003e 0.000\n  slot  2  upgrade  4  attr 396  0.899 -\u003e 0.000\n\neach run twice, once on money the game issued and once with 200 credits written\nonto m_nCurrency the way MvM_DropCredits writes a Cash Bundle.\n\nSo two things are settled. Bundle money does not stop a sale, and this is not\napw-4cb. And the sale is not broken for the player attributes or for any of the\nthree weapon slots, which was the obvious asymmetry to suspect: a weapon upgrade\nlives on a different entity and comes off a different path, and it comes off.\n\nWhat was NOT tested, in the order I would try them.\n\n1. The station's own sequencing. Every probe here sells inside the same\n   MvM_UpgradesBegin/MvM_UpgradesDone session that bought. A player who buys,\n   lets the session close, and sells after reopening the station is doing\n   something this has not done, and the report says \"before leaving the upgrade\n   station\" rather than \"in one go\".\n2. Whether the player was ever right. The server state is correct in all ten\n   runs, so somebody watching an upgrade \"stay bought\" may be watching a station\n   panel that did not refresh. That is a cheap thing to check next play session\n   and it would explain the report without a server bug at all.\n3. The plugin was loaded but not connected to a room. Unlocks_ClassesEnforced\n   and Unlocks_SlotsEnforced gate on having a run, so the enforcement that\n   strips and re-gives weapons almost certainly never fired. \"Plugin present\" is\n   not \"plugin working\", and only the second one is a player's server.\n4. One tier at a time, on a Medic. Multi-tier and other classes are untouched.\n\nP0 to P1: the mechanism everybody suspected is dead and the leading explanation\nleft is cosmetic, but a play-tester did report it and nothing here proves them\nwrong.\n\nThe probe is testbed/stats/mvmbots_refund.sp, run between waves as\n`mvmbots_refund_probe \u003ccredits\u003e [host|bot] [slot]`, results in\naddons/sourcemod/logs/mvmbots_refund.jsonl. Load the plugin with\nTESTBED_ARCHIPELAGO_SMX=\u003cpath to tf2_archipelago.smx\u003e.","created_at":"2026-08-26T09:37:26Z"}],"dependency_count":1,"dependent_count":0,"comment_count":4}
{"_type":"issue","id":"apw-csq","title":"\"Let the mod pick\" picks classes that are unticked","description":"Peppy: \"the bot seats I set as 'Let the mod pick' still picks from classes that\nI have unchecked in the Classes tab.\"\n\nTwo faults, both in the launcher, both about what a draw seat is worth.\n\nA seat is a place in `sm_redbots_manager_team_composition`, counted from one,\nand the mod reads a seat it cannot parse as a seat it should leave to the\nlineup mode. Both settings pages stored the classes somebody named and dropped\nthe draws, so naming seat 4 and leaving the first three to the mod stored one\nclass, and the mod filled it as seat 1. The loadout file numbers seats from the\nsame list, so the Wrangler went to whichever engineer came first rather than to\nthe seat that asked for it.\n\nAnd a team of nothing but draws stored nothing, which is an empty convar, and\nan empty convar is what makes the mod fall back to the map's own default\nlineup. It then treats that default as a team somebody named, and a named team\nbeats the blacklist: `IsBotClassBlacklisted` returns false for any class the\ncomposition mentions. So the map's guess played the classes the Classes tab had\nunticked, which is exactly the report.\n\nFixed. A draw seat is an empty entry rather than a missing one, end to end: the\nwindow, the console form, `SRCDS_BOT_TEAM_COMP` and `SRCDS_BOT_SEAT_LOADOUTS`\nthrough `SplitSeats`, and `Composition`, which writes `engineer,,heavyweapons`\nand keeps seat 3 on 3. Trailing draws name no seat and are still dropped. When\nevery seat is a draw and some class is unticked, the seats go out as holes\nanyway, so the string is not empty and the map's guess never gets to overrule\nthe ticks.\n\nTested where the bug was: `botloadout` for the convar and for the loadout\nfile's seat numbers agreeing with it, `settings` for the environment\nround-trip, `tui` for a seat keeping its number when the seat before it goes\nback on the draw, and `gui` for what the Bots tab means, which is now a plain\nvalue the Win32 widgets are read into so it can be tested off Windows.\n\nWhat is still open is the mod's own rule, and it belongs there: a lineup the\nmap guessed at should not beat a blacklist somebody set. tf2-mvm-bots item 7.","notes":"Imported from TODO.md item 9 — section: Bugs, most impactful first\nOriginal heading: \"Let the mod pick\" picks classes that are unticked. Fixed here","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T13:52:58Z","created_by":"m-this","updated_at":"2026-08-25T13:52:58Z","closed_at":"2026-08-25T13:52:58Z","close_reason":"Closed","labels":["todo-import"],"comments":[{"id":"01a03943-d8a9-775f-b077-aae4ba00b649","issue_id":"apw-csq","author":"m-this","text":"Original report, Peppy: \"It also seems that the bot seats I set as 'Let the mod pick' still picks from classes that I have unchecked in the Classes tab\".","created_at":"2026-08-25T14:12:23Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"apw-hut","title":"RED gets nine bots when the mission loads","description":"Peppy: \"the server is putting 9 bots on red team sometimes, it happens when the\nmission is loaded but once the wave is restarted it fixes itself.\"\n\nOne ask adds more than it asked for, and it is the mod that does the adding.\n`sm_addbots N` runs `AddBotsBasedOnLineupMode(N)`, which calls\n`AddBotsFromTeamComposition(N)` first: that adds the seats the named team still\nwants and reports success only when it filled all N. A team that does not name\nevery seat cannot, so the caller falls through to the lineup mode and adds N\nmore on top. Three named seats and an ask for six is three plus six.\n\nWhich is nine, and it is nine at mission load rather than mid-mission because\nthat is the one moment RED is empty and the whole six are asked for at once.\n\nWhy the wave restart cleared it: the only trim this plugin ran was scheduled\nthree seconds after `mvm_begin_wave`, so between the map loading and the first\nwave nothing counted the team at all.\n\nFixed here, which is the half this repository owns. `Bots_Fill` runs\n`Bots_TrimSurplus` on its own poll now, so RED comes back to its size within\nthree seconds wherever the surplus came from, and the debug log says who left.\n\nThe other half is the mod's and it is the actual bug:\n`AddBotsBasedOnLineupMode` has to subtract what the composition already added\nbefore it falls back. It is filed as tf2-mvm-bots item 18, with the test-bed\nrepro, because a fix there is worth more than a trim here: a bot that is added\nand kicked three seconds later still spent a seat and still cost the wave its\nupgrade time.","design":"## Half of this is fixed, and by two different beads\n\n**The symptom, by apw-49k.** The trim only ever ran three seconds after\n`mvm_begin_wave`, which is why nine bots survived from map load until a wave\nrestart. `Timer_TrimBots` is now armed after `sm_addbots` as well, so a surplus\nat map load is counted back within `BotTrimDelay` instead of waiting for a wave.\nRED still reaches nine for a moment; it no longer stays there.\n\n**The cause, by apw-ygn, but only with a blacklist.** This bead's own reading is\nthat `AddBotsFromTeamComposition(N)` reports success only when it filled all N,\nand a team that does not name every seat cannot, so the caller falls through to\nthe lineup mode and adds N more. apw-ygn made `Composition` pad the lineup out\nto the full team whenever the blacklist is non-empty, so in that case every seat\nis named, N can be filled, and the fall-through never happens.\n\nWith no blacklist a short lineup is still left short on purpose, so\n`[\"scout\"]` against a six-man team still names one seat and the fall-through\nstill fires. That is the remaining half.\n\n## What is left\n\nDecide whether a short lineup should always be padded, or only when a blacklist\nmakes it necessary.\n\nPadding always looks harmless — an empty seat means \"the mod draws this one\",\nwhich is what an unnamed seat meant anyway — and it would close the\nfall-through for good. But it changes what the convar says in the no-blacklist\ncase, and `Composition` documents an empty string as \"the mod plays the map's\nown default lineup\". Whether six empty seats and no string are the same thing to\nthe mod is not established here, and guessing at it is how the padding got\nwritten narrowly in the first place.\n\nThe testbed answers it: set a one-class lineup with no blacklist, count RED at\nmap load, then again with the padding widened.","notes":"Imported from TODO.md item 8 — section: Bugs, most impactful first\nOriginal heading: RED gets nine bots when the mission loads. Half fixed","status":"closed","priority":1,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T13:52:57Z","created_by":"m-this","updated_at":"2026-08-26T21:19:22Z","started_at":"2026-08-25T13:53:41Z","closed_at":"2026-08-26T21:19:22Z","close_reason":"Closed","labels":["todo-import"],"comments":[{"id":"01a03943-d697-7af5-b0bc-99619236a555","issue_id":"apw-hut","author":"m-this","text":"Original report, Peppy on 1.9.0 release day: \"the server is putting 9 bots on red team\" / \"I should mention this happens when the mission is loaded but once the wave is restarted it fixes itself\".","created_at":"2026-08-25T14:12:22Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"apw-jo7","title":"A Cash Bundle heals the player it pays","description":"Reported 2026-08-27, and not intended.\n\nMvM_DropCredits pays a bundle by spawning an item_currencypack_custom at the\nplayer's feet and letting them walk into it, which is how the game itself pays.\nA currency pack in MvM heals whoever picks it up. That is the game's behaviour,\nnot ours: nothing in this repo asks for it, and it is not from kelly-cs's weapon\nbuff work, which touches no currency code.\n\nSo every Cash Bundle is worth its credits plus a free top-up, and the bigger the\nbundle the less that matters, which is backwards.\n\n## Why the obvious fix is not free\n\nMvM_DropCredits already has a second path that writes m_nCurrency directly, used\nwhen a pack cannot be told its amount. That one does not heal. But the comment\nabove it says why the pack is tried first: money written straight onto\nm_nCurrency is money the game's own record never saw, so the refund button and\nthe wave-loss restore do not know about it. That is what g_BundleCredits and the\nbundle ledger exist to paper over.\n\nSwapping the order trades a heal nobody asked for against an accounting hole\nthat has already produced bugs. Both are worth measuring before choosing.\n\n## Other ways out\n\n- spawn the pack and suppress the heal, if a property or a hook on the pickup\n  can do it, which nobody has checked\n- pay through the pack and take the healed amount back off, which is exact but\n  visible as a flicker\n- decide the heal is small enough to keep and write it down as intended, which\n  is the one option that needs no code\n\nWorth measuring first: how much a pack actually heals, since none of the above\nis worth doing if it is a handful of health.","status":"closed","priority":2,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-27T13:59:33Z","created_by":"m-this","updated_at":"2026-08-27T15:36:05Z","started_at":"2026-08-27T15:28:49Z","closed_at":"2026-08-27T15:36:05Z","close_reason":"Kept on purpose, decided with the numbers in hand.\n\nThe heal is Scouts only, 25 points of decaying overheal per pile between waves,\nand the same whether the bundle is worth 50 credits or 1000. On servers where\nitem_currencypack_custom carries no amount property the pack path never runs at\nall and the fallback heals nothing.\n\nAll three fixes cost more than that. Reordering the payment paths pushes every\nbundle through g_BundleCredits and widens the surface that produced apw-6hf.\nSuppressing the heal has no hook, since the pickup is the payment. Subtracting\nthe healed amount reads health across a frame boundary against overheal the game\nis already decaying.\n\nRecorded in the doc block above MvM_PackCredits so the next reader does not file\nthis again. Reopen if a Scout-heavy team ever makes 25 points matter.","comments":[{"id":"01a043d5-4959-7b5b-bd4f-7954e148cab8","issue_id":"apw-jo7","author":"m-this","text":"Picked up for a fix. The agent is told to establish how much a pack actually\nheals before choosing between the options, since none of them is worth doing for\na handful of health, and to treat reordering the two payment paths as a trap\nrather than the obvious answer: that is the accounting hole apw-6hf came out of.","created_at":"2026-08-27T15:27:27Z"},{"id":"01a043d6-8d23-7c8f-b306-e7427c8060ff","issue_id":"apw-jo7","author":"m-this","text":"Measured the heal from the game's own rule rather than the server: only Scouts are healed by a credit pile, and the amount is per pile, not per credit. 50 health when below full, 25 as overheal at full, dropping to about 5 once a Scout is over 700. Every other class gets nothing.\n\nTwo things shrink that further here. A bundle only pays between waves (MvM_CanPayCredits requires RoundState_BetweenRounds), which is where everybody is standing at the upgrade station at full health, so the realistic case is the 25 overheal, and MvM overheal decays on its own. And the pack path is inert on every server seen so far: MvM_PackCredits gives up when none of m_nAmount, m_nCurrencyAmount or m_iAmount exists on item_currencypack_custom, which is the case the AP_Debug line reports, and the m_nCurrency fallback does not heal at all.\n\nSo the heal is at most 25 decaying overheal on one class in the common case, and zero on the servers where the pack cannot carry an amount. Not worth trading against the accounting hole that produced apw-6hf, and not worth a health read-back and rewrite either. Taking the bead's no-code option: the heal is accepted and written down where the pack is spawned.","created_at":"2026-08-27T15:28:49Z"},{"id":"01a043d7-89ee-7adf-bb4b-6d60fee9dc86","issue_id":"apw-jo7","author":"m-this","text":"Correction: the description above was wrong about who gets healed, and it\nchanges the size of the problem.\n\nCredit piles in MvM heal Scouts only. Every other class gets nothing. The amount\nis per pile, not per credit: 50 when below full, 25 as overheal at full, tapering\nto about 5 above 700 health. So a bundle worth 1000 credits and one worth 50 give\nthe same 25 points.\n\nTwo things shrink it further. MvM_CanPayCredits only pays between rounds, when a\nScout is at the station at full health, so the realistic case is the 25 point\noverheal, which decays on its own. And the pack path is inert on every server\nseen so far: MvM_PackCredits bails when item_currencypack_custom carries none of\nthe three amount properties, and the m_nCurrency fallback heals nothing at all.\n\nThe three fixes all cost more than that. Reordering the paths pushes every bundle\nthrough g_BundleCredits and widens the surface that produced apw-6hf, to claw\nback decaying overheal from one class. Suppressing the heal has no hook: the\npickup is the payment. Subtracting the healed amount means reading health across\na frame boundary against overheal the game is already decaying.\n\nReopened rather than closed because it was reported as unintended and that stands\nuntil the reporter has seen these numbers. The question is now narrow: is 25\npoints of decaying Scout overheal per bundle worth any of the above.","created_at":"2026-08-27T15:29:54Z"}],"dependency_count":0,"dependent_count":0,"comment_count":3}
{"_type":"issue","id":"apw-8b9","title":"Settings window polish: tab name, beta tag, number alignment","description":"Three from the same pass over the settings window.\n\n- 'Who can join (beta)' becomes 'Networking'. The beta tag moves into the docs\n  note that already warns steam is unfinished, which is where it belongs: the\n  tab is where the port lives too, and the port has been played on.\n- The three robot scales read 'Robot damage (%)' and so on, so the number in\n  the box says what it is without the help text.\n- The three balancing number boxes were right aligned. leftAlign already\n  existed for exactly this and they were never passed to it.","status":"closed","priority":2,"issue_type":"task","owner":"m@this.fr","created_at":"2026-08-27T13:34:38Z","created_by":"m-this","updated_at":"2026-08-27T13:34:46Z","closed_at":"2026-08-27T13:34:46Z","close_reason":"All three done, in the GUI and the TUI where each applies.\n\nThe tab is 'Networking' in both interfaces and in the TUI's tab test. Both\ndocs/en and docs/fr invite-your-friends notes were rewritten: the old sentence\ngave the tab name as the reason to distrust steam, which no longer holds, so\nthe warning now stands on its own. Vale clean on STE and FR.\n\nThe percent suffix is on the three labels in both interfaces.\n\nAlignment turned up a second one: leftAlign was already called with botsSize,\nbut botsSize is nil at that point because the Bots tab builds lazily, so the\nteam size box was never aligned either. leftAlign(botsSize) now runs where the\ntab is actually built.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-5tk","title":"Spare checks are cash, and cash is forgettable","description":"What kelly-cs's PR #3 solves, filed so the change has a reason on record rather\nthan only a diff.\n\n## The problem\n\nA run has more checks than it has things worth finding. The ones left over paid\ncash, and cash is the weakest reward the game has: it is spent within the wave\nit arrives in and leaves nothing behind. A player who found a spare check five\nwaves ago cannot point at anything it gave them.\n\n## What the PR does\n\nTurns the spare checks into stackable weapon buffs drawn from the game's own\nitem attributes: damage, reload rate, clip size, projectile count, and the\nsubstance effects across weapon families. They persist for the run, so a check\nfound in wave 2 is still doing something in wave 8.\n\nTwo options with it: WeaponBuffPercentage, how much of the spare space is buffs\nrather than cash, and WeaponBuffStackChance, how often a buff adds a level to\none already drawn instead of a new permutation.\n\n## Worth knowing about the shape of it\n\n- Buffs are on by default. WeaponBuffPercentage is 75, and with cash rewards\n  off every spare check is a buff. Existing runs change shape on upgrade, which\n  is the intent rather than an accident, but it is not opt-in.\n- 227k lines, and 224k of them are generated: items.json, the plugin's key table\n  and the frozen ids. The hand-written part is about 3k.\n- The plugin's data size goes from 19 KB to 2.8 MB, all of it the static key\n  table for 212 weapons by 80 effects. Memory, not frame time: there is no\n  OnGameFrame in it and the hooks are event driven.\n\n## Follow-ups the author names\n\nMore custom upgrades beyond the vanilla attribute list.","status":"closed","priority":2,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-27T08:46:48Z","created_by":"m-this","updated_at":"2026-08-27T09:07:59Z","closed_at":"2026-08-27T09:07:59Z","close_reason":"Merged as PR #3 and shipped. The Balancing tab in both interfaces carries the options, on by default at 75 per cent of spare checks. CI on the merged code caught one test the branch had never run; fixed in d7b32eb.","comments":[{"id":"01a04266-be70-77cb-b56c-d358efcd0d81","issue_id":"apw-5tk","author":"m-this","text":"Implemented by kelly-cs in https://github.com/m-this/tf2-archipelago/pull/3","created_at":"2026-08-27T08:47:05Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"apw-e45","title":"A cancelled nightly leaves a half-pushed image, and the next one fails on unknown blob","description":"Run 32995004890 failed on `build and push the archipelago image`:\n\n    unknown blob\n    ##[error]Process completed with exit code 1\n\n(The `ModuleNotFoundError: No module named 'requests'` lines above it are\nArchipelago's own optional apworlds failing to import. They are normal and are\nnot this.)\n\n## Cause\n\n`unknown blob` is the registry being asked for a layer it does not hold whole.\nThe nightly makes that likely by design:\n\n    concurrency:\n      group: nightly\n      cancel-in-progress: true\n\nThree runs around the failure were cancelled: 0b82ec8d, 54ca4539, 22ab60cd. A\nrun cancelled part-way through `docker push` leaves the blob it was uploading\nincomplete in the registry, and the next run pushing the same layer refers to\nit and gets this.\n\nThe run after it succeeded on its own, which fits: a fresh push re-uploads the\nlayer and the reference resolves. So this is intermittent and self-healing,\nwhich is the worst kind of red in a log, because it teaches everybody to ignore\na failed nightly.\n\nGitHub also had a major Actions incident the same afternoon, so registry\nflakiness on its own cannot be ruled out as a second cause. Either way the fix\nis the same.\n\n## Fix\n\nRetry the push. It is the whole answer for both causes: a re-push re-uploads\nthe missing layer, and a registry having a bad minute usually stops having one.\n\nThree steps push (`bridge`, `srcds`, `archipelago`) and each writes the same\ntwo lines, so a small helper doing build-then-push-with-retry covers all three\nand stops the retry being added to one and forgotten on the others. Bound it:\nthree attempts, a few seconds apart, and let the step fail on the third so a\ngenuine registry refusal still shows.\n\nDo not drop `cancel-in-progress`. It is there because two runs racing would\nboth delete and recreate the `nightly` tag, and the loser would leave the\nrelease describing a commit the tag no longer points at. That is worse than a\nretried push.","status":"closed","priority":2,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T18:00:50Z","created_by":"m-this","updated_at":"2026-08-26T18:02:42Z","started_at":"2026-08-26T18:00:59Z","closed_at":"2026-08-26T18:02:42Z","close_reason":"Fixed. deploy/push-image.sh retries a push three times with a widening delay, and both workflows call it so the retry cannot be added to one image and forgotten on the others. Exercised against a stub docker in both directions: recovers when the third attempt succeeds, and still exits 1 with 'push failed 3 times' when every attempt fails. cancel-in-progress deliberately left alone.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-7ts","title":"Scale the run's difficulty to how few humans are on the slot","description":"Peppy, on Discord:\n\n\u003e How about an upgrade multiplier that adds up depending on how few humans are\n\u003e playing on the slot? So less players mean that the effect of an upgrade you\n\u003e buy is increased but the cost stays the same\n\nAnd the shape it should take, from the same thread:\n\n\u003e Peppy: Maybe could be an option you set in the yaml\n\u003e Peppy: Or it could even be an unlock\n\u003e omaewh4: that's smart yeah\n\n## Why this and not better bots\n\nThe reason it is worth doing at all, from the thread:\n\n\u003e omaewh4: I think having a way to balance the difficulty without relying on\n\u003e bots AI is really important, like damage multipliers, more health, etc\n\nEvery other lever on difficulty here is the bot mod playing better, which is\nslow, uncertain, and measured a wave at a time. A multiplier is none of those.\nIt moves the whole curve, it is one number, and it works the same on a mission\nnobody has tuned. It also covers the case the bots never will: a solo player\nagainst a mission written for six people.\n\n## What Peppy is asking for exactly\n\nThe upgrade keeps its price and gains effect. That is deliberate and it is the\nbetter half of the idea: it leaves the credit economy alone. Prices are what\nmissionsanity, the wave payouts and the Cash Bundle item all balance against,\nand a discount would move every one of them. An effect multiplier moves none.\n\nScale it on the humans on the slot, not on RED's size. The bots fill RED to six\nwhatever happens, so RED's size answers the same on a solo run as on a full one\nand is useless as a signal.\n\n## The two ways to expose it, and they are not exclusive\n\n**A yaml option** is the plain one: a number in the player file, read at\ngeneration, applied for the whole run. Predictable, and it is how somebody sets\nthe difficulty they want before they start.\n\n**An unlock** is the interesting one, and it is the one that belongs in a\nrandomiser. The multiplier arrives as an Archipelago item, so the run gets\neasier as the multiworld hands it over, and a seed can gate it behind another\nworld. That gives the same knob a place in the item pool instead of a settings\npage.\n\nBoth can exist: the yaml sets the floor, the items raise it. Decide the floor\nfirst, because an item that raises a number nobody set has nothing to raise.\n\n## Open questions before any of it is built\n\n- Which effect. \"Upgrade effect\" is not one number in Mann vs Machine: damage,\n  fire rate, resistances and the building upgrades are separate attributes. The\n  smallest honest version is a damage multiplier and a resistance multiplier,\n  not a single global one.\n- Whether it recomputes when somebody joins or leaves mid-run. A player joining\n  should probably not weaken the team mid-wave, so recompute between waves.\n- Whether it counts humans on the server or humans on RED. A spectator is not\n  playing.\n- How it shows. A run silently 40 per cent easier is a run nobody trusts; the\n  session tab should say what multiplier is live and why.\n\nRelated: apw-no9 (Progressive Starting Money) is the same idea aimed at the\neconomy rather than the effects, and the two should not both be turned up\nwithout measuring, or a solo run is easy twice over.","status":"open","priority":2,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-26T17:20:56Z","created_by":"m-this","updated_at":"2026-08-26T17:20:56Z","labels":["discord"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-q5v","title":"A nightly build calls itself by the release version it was built past","description":"The window title on a nightly reads:\n\n    Mann vs Archipelago  1.9.0-837b556\n\nThat build is not 1.9.0. It is main, some commits past the 1.9.0 tag, and it\nsays so nowhere a player looks.\n\n## Why it says that\n\n`Makefile:16` takes `RELEASE_VERSION` from the apworld's `world_version`, which\nis the last release the apworld declares. The launcher string was then\n\n    assets.LauncherVersion=$(RELEASE_VERSION)-$(BUILD_COMMIT)\n\nso every build between releases carries the previous release's number. That is\nright for a release build and wrong for a nightly, and the two were the same\nexpression.\n\n## What it costs\n\nBug reports. A player on a nightly says \"I am on 1.9.0\", and every triage of\nthat report starts from the released code. Today's session hit this from the\nother side: two bundles were read as 1.9.0 and the launcher line in both said\n`1.9.0-c42e61c`, which is only distinguishable by knowing that `c42e61c` is not\nthe release commit. Nobody knows that from a chat message.\n\nThe short commit is in there and it is definitive, but it only helps somebody\nwho already suspects the build is not what it claims.\n\n## Fix\n\nA `BUILD_CHANNEL` variable, empty by default:\n\n    BUILD_CHANNEL ?=\n    LAUNCHER_VERSION := $(if $(BUILD_CHANNEL),$(BUILD_CHANNEL),$(RELEASE_VERSION))-$(BUILD_COMMIT)\n\n`.github/workflows/nightly.yml` passes `make dist BUILD_CHANNEL=nightly`, so a\nnightly titles itself `nightly-837b556`. `release.yml` keeps plain `make dist`\nand is unchanged, and so is a local build.\n\nOnly the displayed string moves. `RELEASE_VERSION` stays as it was because the\nWindows version resource takes it as `$(RELEASE_VERSION).0` through\ngoversioninfo, and that field has to be four numbers: a channel name in it would\nnot build.\n\nThe same string reaches `summary.txt` in the debug bundle through the assets\nversion map, so the next bundle from a nightly says which it is on its own.","status":"closed","priority":2,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-26T15:52:23Z","created_by":"m-this","updated_at":"2026-08-26T15:52:53Z","closed_at":"2026-08-26T15:52:53Z","close_reason":"Fixed. BUILD_CHANNEL is empty by default so release and local builds are unchanged; nightly.yml passes BUILD_CHANNEL=nightly and the title becomes nightly-\u003csha\u003e. Verified the Windows -file-version resource stays 1.9.0.0 with the channel set, since goversioninfo needs four numbers.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-gv9","title":"The connect address is not on the session tab","description":"Cowser, on native Linux, running 1.9.0:\n\n\u003e also, there's no clear display of where the IP is to connect unless you click\n\u003e C on the log screen :P\n\u003e so def some more visibility on how to connect on the sessions tab?\n\nThe address exists, and the launcher already knows it. It is only reachable by\npressing C on the log tab, which is a keystroke nobody finds without being told.\n\nThe session tab is where a player looks for it, and it is where the server's\nown state already lives. Put the connect line there, and make it copyable in\nplace rather than only from the log.\n\nHis bundle has `srcds_reach: \"port\"` and port 27015, so this is the plain\ndirect-connect case, not the SDR one. Whatever the tab shows must still be\nright for `reach=sdr`, where the FakeIP is what a player needs and the LAN\naddress is useless to them.\n\nWorth showing the whole `connect \u003caddr\u003e` command rather than the bare address,\nsince that is what gets pasted into the TF2 console. If a server password is\nset, the command needs it too, and the session tab is the one place the\nlauncher can say so without printing the password to a log.","status":"closed","priority":2,"issue_type":"task","owner":"m@this.fr","created_at":"2026-08-26T12:29:02Z","created_by":"m-this","updated_at":"2026-08-26T17:21:40Z","started_at":"2026-08-26T17:18:09Z","closed_at":"2026-08-26T17:21:40Z","close_reason":"Fixed for the terminal, which is where the gap was. The Windows window already carried the join addresses on its Session tab; the TUI only had a truncated header list and the C key on the log tab, and the report came from a native Linux player. The session tab now renders ConnectLines: the connect command rather than a bare address, untruncated, with what each reach still needs and the server password when set. No test: the TUI session view needs a running supervisor to render, and ConnectLines has its own tests in internal/runtime. Worth a visual check on a real run.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-swv","title":"Cut the README down and give it a shape","description":"252 lines, fifteen headings. Copy https://github.com/gastownhall/beads:\nheadline, quick start, features, a diagram, then links out. Under a hundred\nlines.\n\n## Cut\n\nEighty two lines are install instructions that docs/en/setup/install-windows.md\nand install-linux.md already carry. Two copies of a procedure is one that goes\nstale, and this is the copy nobody edits.\n\nHow it works, Directory tree, Why MvM and Code signing policy are documentation\nwearing a README. The tree will be wrong within a month.\n\nWhat is cut moves to a docs page. Credits stays, it names people. Code signing\nmoves but stays linked: SignPath asks for a public policy (apw-gb8).\n\n## Add\n\nA quick start above the fold. The first instruction is on line 45 under a\nWindows heading, so a Linux reader scrolls past it.\n\nA feature list: bold label, one line, about eight.\n\nThe Archipelago list, missing entirely, and what an AP player reads first.\nCheck the numbers against the tree before publishing them:\n\n- checks, four kinds (gamedata/locations.go): wave cleared, mission cleared,\n  tank destroyed, giant killed, across 29 missions and 181 waves\n- items, 40, of which 39 progression and 1 filler: Mission Ticket per mission,\n  Class per mercenary, Progressive Weapon Slot, Cash Bundle at 200 credits\n- goals: Final Boss, or Missionsanity for a share of the missions\n- options worth naming: mission count, difficulty pool, excluded missions,\n  starting mission, starting class\n\nA diagram nobody has drawn: game, plugin, bridge, room. Mermaid, so both forges\nrender it.\n\nDocumentation as a list of links. 47 files under docs/, linked 19 times now.\n\n## Then\n\nRun it through /rewrite. It is the front page and the first English many players\nread, and the changelog and release notes already had that pass. docs/fr needs\nthe same rather than being left as the long version.\n","status":"closed","priority":2,"issue_type":"task","assignee":"m-this","owner":"m@this.fr","created_at":"2026-08-26T11:34:47Z","created_by":"m-this","updated_at":"2026-08-26T11:50:54Z","closed_at":"2026-08-26T11:50:54Z","close_reason":"README is 145 lines from 252, with a quick start, a feature list, the Archipelago list, a mermaid diagram and a links section. What was cut moved to repository.md, code-signing.md and licences.md in both languages, or was deleted where the setup guides already carried it. Vale clean, book builds.","dependencies":[{"issue_id":"apw-swv","depends_on_id":"apw-gb8","type":"related","created_at":"2026-08-26T12:35:05Z","created_by":"m-this","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-768","title":"Lock an Australium Medal onto every mission clear","description":"Split out of apw-o75, which carried it as \"what is still open\". It is a design\nchange with a data format bump behind it, not the tail of a bug fix, so it waits\non its own bead rather than holding that one open.\n\nThe goal currently reads a list of locations this server checked itself. That\nworks. The better shape, which the original play-test report suggested, is to\nstop asking about locations at all:\n\n- Lock a trophy item, an Australium Medal, onto every mission clear. No other\n  player's item is ever under a clear, so !collect cannot touch one.\n- The goal becomes state.has(\"Australium Medal\", player, target) rather than\n  can_reach_location, which is what generation logic wants anyway.\n\nCosts:\n\n- Eight locations' worth of other people's items. A mission clear is one of the\n  better checks this world offers, and locking it means the multiworld loses it.\n  That is the real argument against, and it is why this is not obviously right.\n- An item id, so a gamedata change on the Go side, a regenerated data.py, and a\n  data format bump. Seeds and saves on the old format do not survive it.\n\nDo it the next time the format moves for another reason, so the break is paid\nfor once. Not on its own.\n","status":"open","priority":2,"issue_type":"feature","assignee":"m-this","owner":"m@this.fr","created_at":"2026-08-26T08:29:47Z","created_by":"m-this","updated_at":"2026-08-26T08:29:47Z","dependencies":[{"issue_id":"apw-768","depends_on_id":"apw-o75","type":"discovered-from","created_at":"2026-08-26T09:30:00Z","created_by":"m-this","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-4bt","title":"Mission progress shows other people's collect as your own clears","description":"Peppy, confirming the apw-o75 fix and naming what it left behind:\n\n\"it seems that other worlds collecting their remaining items on goal complete\nstill mark your missions as cleared without having actually cleared them\nyourself\"\n\"but i guess it doesnt actually complete your own goal on itself anymore it\nseems so thats good\"\n\"But it does make it harder to keep track of what you've actually completed or\nnot\"\n\napw-o75 split played from checks and pointed the goal at played. The goal is\nright now. What still reads checks is everything the player looks at: a mission\nanother player's !collect touched is drawn as cleared, so the list no longer\nsays what this player did.\n\nSame fix, one level up. Anything that reports this player's own progress reads\nplayed; !checked keeps reading checks, because reporting what the room has is\nits whole job. Where both matter, show them apart rather than merging: cleared\nby you, versus checked by the room.\n\nCheap, no data format change, and it does not wait on the Australium Medal work\nin apw-o75.\n","status":"closed","priority":2,"issue_type":"bug","assignee":"m-this","owner":"m@this.fr","created_at":"2026-08-26T06:46:39Z","created_by":"m-this","updated_at":"2026-08-26T17:33:19Z","started_at":"2026-08-26T17:30:20Z","closed_at":"2026-08-26T17:33:19Z","close_reason":"Fixed. /missions serves played alongside cleared, and the GUI and TUI both draw them apart: 'cleared' means this server played it, 'collected' means only the room holds the check. Cleared keeps its old meaning because the plugin chains on it. Test covers the disagreement directly. Not seen against a live room with another world collecting.","labels":["discord"],"dependencies":[{"issue_id":"apw-4bt","depends_on_id":"apw-o75","type":"discovered-from","created_at":"2026-08-26T07:46:45Z","created_by":"m-this","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-4zj","title":"No weapons when the item server is unreachable, with no message saying so","description":"Cowser: \"was the item servers just down yesterday or do you need to do something\nspecific to have weapons in the server\" / \"bc i couldnt connect to the item\nserver and had to run full stock on me and my bots\"\n\nHe could not tell whether it was an outage or a missing setup step, which is the\nactual defect: the fallback to full stock is silent. Whether the fetch belongs\nhere or on the mod side is unresolved.","status":"closed","priority":2,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T14:12:11Z","created_by":"m-this","updated_at":"2026-08-26T17:39:45Z","started_at":"2026-08-26T17:36:49Z","closed_at":"2026-08-26T17:39:45Z","close_reason":"Fixed the silent half, which the bead names as the actual defect. ItemServerLine matches the 'Current item schema is up-to-date' success line, verified against two real bundles, and passes anything else about the schema through without interpreting it. The GUI prints it; the TUI keeps it on the session tab so it does not scroll away. Whether the fetch itself belongs here or on the mod side is still unresolved and out of scope for the message.","labels":["discord"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-d8b","title":"Item and location names print as raw IDs","description":"Cowser: \"Also it didn't print names of items or locations, just IDs\"\n\nSame session as his crash reports, so possibly a client that never received the\ndatapackage rather than a formatting bug. Not diagnosed.","status":"closed","priority":2,"issue_type":"bug","owner":"m@this.fr","created_at":"2026-08-25T14:12:11Z","created_by":"m-this","updated_at":"2026-08-26T17:28:17Z","started_at":"2026-08-26T17:24:56Z","closed_at":"2026-08-26T17:28:17Z","close_reason":"Fixed, and the bead's guess was wrong: not a client that missed the datapackage, but the bridge never asking for one. printJSON.text read only the Text field, which carries the id itself for typed parts. The bridge now sends GetDataPackage on connect for the games in the room, keeps names per game because two games may both number an item 77, and takes player names free from Connected. Five tests cover resolution, the game-picking, unresolved and ambiguous ids, and the pre-handshake case. Not seen against a live room: worth one look at chat on the next run.","labels":["discord"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-7v6","title":"The engine watchdog does not exist on Windows, so it cannot explain the crash there","description":"Recorded so the question is not reopened. It came up because the Windows crash\nin apw-r85 looked like it might be the watchdog, and because disabling it on\nWindows the way apw-4cb did on Linux sounds reasonable until you look.\n\n## The watchdog is POSIX only\n\nFrom the TF2 tier0 source. On POSIX it is real:\n\n    PLATFORM_INTERFACE void Plat_BeginWatchdogTimer( int nSecs )\n    {\n        ...\n        nSecs *= s_nWatchDogTimerTimeScale;\n        nSecs = MIN( nSecs, 5 * 60 );\n        if ( nSecs )\n        {\n            s_WatchdogTime = nSecs;\n            signal( SIGALRM, WatchDogHandler );\n            alarm( nSecs );\n        }\n    }\n\nOn Windows all four of `Plat_BeginWatchdogTimer`, `Plat_EndWatchdogTimer`,\n`Plat_GetWatchdogTime` and `Plat_SetWatchdogHandlerFunction` are empty bodies.\nIt is `alarm()` and `SIGALRM`, and Windows has neither.\n\nThe local Linux install agrees: `-nowatchdog` appears in `bin/libtier0.so`,\n`bin/libtier0_srv.so` and `bin/linux64/libtier0_srv.so`, and nowhere else in\nthe tree. `libtier0_srv.so` also carries the message,\n`WatchDog! Server took too long to process (probably infinite loop).`\n\nThree consequences:\n\n- Passing `-nowatchdog` on Windows does nothing. Worse than nothing: it reads\n  like a protection is being turned off, so the next person assumes one was\n  there.\n- apw-4cb is correctly scoped and finished. Linux is the only platform with a\n  watchdog to disable.\n- The `0xc0000005` in apw-r85 is not the watchdog. That was the leading guess\n  and it is now ruled out, which leaves the access violation needing a real\n  cause and the minidump to find it.\n\n## There is no softer setting\n\nWorth writing down, because \"make it less aggressive\" is the obvious next idea.\n\n- The flag is parsed as `strstr( g_CmdLine, \"-nowatchdog\" )`. It is on or off.\n- The timeout is whatever the engine passes, capped at `MIN( nSecs, 5 * 60 )`.\n- `s_nWatchDogTimerTimeScale` is the only knob and it is not exposed: 10 in a\n  debug build, 1 in a release one.\n\nNothing on the command line moves the threshold. The comments in\n`launcher/internal/runtime/runtime.go` now say all of this at both branches.","status":"closed","priority":3,"issue_type":"chore","owner":"m@this.fr","created_at":"2026-08-26T14:14:18Z","created_by":"m-this","updated_at":"2026-08-26T14:14:28Z","closed_at":"2026-08-26T14:14:28Z","close_reason":"Recorded, nothing to build. The finding is in the bead and in the comments at both branches of srcdsArgs.","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-s9b","title":"docs/fr/glossary.md is a second copy of CONTEXT.md","description":"CONTEXT.md is the glossary and the first file an agent reads, which is why\nCLAUDE.md names that path. docs/fr/glossary.md is a translation of it, 200-odd\nlines, and nothing keeps the two in step: a term added to one is missing from\nthe other until somebody notices.\n\nThe English side no longer has this problem. docs/en/SUMMARY.md used to link a\nglossary.md that never existed, and the page written for it points at\nCONTEXT.md rather than copying it.\n\nThe French cannot do the same, because the target is in English. So either the\ntranslation is accepted as a real document and gets a line in CONTEXT.md saying\nit must be updated too, or docs/fr/glossary.md becomes a page that says the\nglossary is in English and links it.\n\nThe first if the French book is meant to stand alone, which the rest of docs/fr\nsuggests it is. Then the duty belongs in CONTEXT.md where the editor will see\nit, not in the translation where they will not.\n","status":"closed","priority":3,"issue_type":"task","assignee":"m-this","owner":"m@this.fr","created_at":"2026-08-26T11:50:56Z","created_by":"m-this","updated_at":"2026-08-26T17:08:45Z","started_at":"2026-08-26T17:07:12Z","closed_at":"2026-08-26T17:08:45Z","close_reason":"Fixed. docs/fr/glossary.md is now the same pointer to CONTEXT.md that the English page is, 209 lines down to 15, with a paragraph saying why the terms stay in English. Vale clean on FR rules, make docs-build exit 0.","labels":["docs"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-p2v","title":"Keep the pinned versions current","description":"Standing task for the /upgrade-deps sweep, so a version bump is tracked rather\nthan remembered. Everything this project pins is in deploy/env/versions.env by\ndesign, except the Go version, which is the `go` line in go.mod because that\ndirective must be a literal.\n\n## Done 2026-08-26\n\nGo 1.26.6 to 1.27.0, in go.mod, deploy/Dockerfile.bridge and the ci.yml\ncontainer image. golangci-lint v2.12.2 to v2.13.1, ruff 0.16.3 to 0.16.4,\npillow 12.1.0 to 12.3.0, nginx 1.30.4-alpine to 1.31.4-alpine. `go get -u ./...`\nmoved five indirect deps, golang.org/x/text v0.3.8 to v0.41.0 among them.\ngofumpt, govulncheck, goversioninfo and honkit were already current.\n\nThe Go bump was not optional. golangci-lint v2.12.2 carries staticcheck v0.7.0,\nwhich panics on the Go 1.27 standard library: \"buildir: package poll: unexpected\nexpr: *ast.KeyValueExpr\". `make lint` could not run at all on a machine with\n1.27 on the path. v2.13.1 fixes it.\n\nWith lint running again it found three embedlit hits, and `go fix` in 1.27 knows\nthe rewrite: an embedded struct's fields go straight in the outer literal, so\n`actionField: actionField{label: \"Repair\"}` becomes `label: \"Repair\"`. Applied\nwith `go fix ./...`. Worth knowing for the next bump that guessing that syntax\ngets \"missing type in composite literal\"; run the tool rather than reading the\nmessage.\n\n## Held on purpose, with the reason\n\nThese are the ones a sweep must not bump on sight.\n\n  node 24-alpine     24 is the LTS. 26 exists and is Current, not LTS.\n  python 3.13-slim   Archipelago 0.6.7 supports 3.11 to 3.13. 3.14 is not a\n                     version the interpreter this world runs in accepts.\n  ACTIONS_VERSION    v3.9.2. v4.0.0's TF2 source was never ported to the new\n                     processor API and does not compile.\n  the game stack     SourceMod, Metamod, ripext, cbasenpc, defenderbots and\n                     ARCHIPELAGO_VERSION each have a comment next to them in\n                     versions.env saying what breaks. Read it first.\n\n## Open, and not done here\n\nactions/upload-artifact is on v4 and v7 is out, three majors. It is used by one\nstep, staging the exe for SignPath, and that path is switched off until the\nSignPath application is granted (apw-gb8). So the bump cannot be verified by\nrunning it, and a major action bump nobody can test is worse than a stale one.\nDo it when apw-gb8 unblocks and the step runs.\n\ngcr.io/distroless/static-debian12 could be debian13. The bridge is a static\nbinary so the base does almost nothing, which is the argument both ways. Not\nurgent.\n\n## Drift worth fixing once\n\nversions.env says the Go version is derived by every consumer with\n`sed -n 's/^go //p' go.mod`. Dockerfile.bridge and ci.yml do not derive it, they\nhardcode the tag, which is why this bump had to touch three files instead of\none. Making them read go.mod would make the next Go bump a one-line change.\n","design":"## The drift this bead kept re-creating is now caught\n\nThree places carried the Go version and only one could be read by the others.\n`go.mod` owns it, the two release workflows already take it with\n`go-version-file: go.mod`, and two pins could not: the `FROM` line in\n`deploy/Dockerfile.bridge` and the container image in `.github/workflows/ci.yml`.\n\nBoth were hand-edited on every bump. Forgetting one is not harmless: the\n2026-08-26 sweep found golangci-lint v2.12.2 carrying a staticcheck that panics\non the Go 1.27 standard library, so a tree on 1.27 with a builder still on 1.26\ncould not lint at all, and the failure read as a linter bug rather than a stale\npin.\n\n`deploy/check-go-version.sh` compares both against the go directive and `make\ncheck` runs it first, so the stale pin is named before anything downstream fails\nfor it. A pin shorter than the directive is accepted, because `golang:1.27`\nserves 1.27.0.\n\nThat does not close this bead. It stays open as the standing task for the next\n`/upgrade-deps` sweep; what changed is that one whole class of mistake inside it\nnow fails loudly instead of quietly.","status":"open","priority":3,"issue_type":"task","assignee":"m-this","owner":"m@this.fr","created_at":"2026-08-26T10:34:54Z","created_by":"m-this","updated_at":"2026-08-26T19:00:08Z","dependencies":[{"issue_id":"apw-p2v","depends_on_id":"apw-gb8","type":"related","created_at":"2026-08-26T11:35:19Z","created_by":"m-this","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-3ht","title":"Document that logic is per mission, not per wave","description":"MarioSpore had to ask three times to get this, which makes it a documentation\ngap rather than a support question:\n\n\"If I have access to a new ticket, how much of a wave am i logically expected to\ndo? UT does not give me any details on what locations are in logic or not when I\nreceive a ticket or a class\"\n\nomaewh4's answer, which is the text worth publishing:\n\n\"Logic works per mission, not per wave. A mission is one region, every check\ninside it sits behind one gate: the waves, the tank, the giant, and the clear.\nSo when a ticket goes into logic, the whole mission goes into logic. There is no\ncase where wave 3 is in logic and wave 6 is not. You do the whole mission.\"\n\nHe had also read rules.py and worked out the ticket tiers himself (Novice: any\nticket; Intermediate: 2 classes and 1 slot; Advanced: 3 classes and 2 slots),\nwhich suggests the game page should state those too.","status":"closed","priority":3,"issue_type":"task","owner":"m@this.fr","created_at":"2026-08-25T14:12:13Z","created_by":"m-this","updated_at":"2026-08-26T17:24:19Z","started_at":"2026-08-26T17:21:46Z","closed_at":"2026-08-26T17:24:19Z","close_reason":"Documented in both languages, in what-the-randomizer-changes.md under the tickets section. Says logic is per mission and publishes the real REQUIREMENTS table from rules.py: five tiers, not the three MarioSpore had inferred, with the class and slot counts. Vale back to the pre-existing baseline in both files, make docs-build exit 0.","labels":["discord"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-c13","title":"The launcher does not update itself","description":"Jebby: \"Does the launcher auto-update or do I have to reinstall, then? I thought\nfor sure it updated today but I might've misread\".\n\nEZKSupernova: \"just download the new .exe and you're good\".\n\nWorks, but every release costs each player a manual download, and Jebby had\nalready lost time to running an old build: he was reporting the bot auto-ready\nbehaviour that a newer version had already changed.","status":"open","priority":3,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-25T14:12:12Z","created_by":"m-this","updated_at":"2026-08-25T14:12:12Z","labels":["discord"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-no9","title":"Feature: Progressive Starting Money","description":"CaptPurpleHeart: \"an option to have 'Progressive Starting Money' items, where\nat the start of every Wave 1, you get X amount more starting money per every\nprogressive starting money upgrade found.\"\n\nSame shape as the three progressives in item 3: a YAML option that is a count,\noff at zero, so a seed that leaves it out keeps meaning what it meant.\n\nIt is blocked on item 1 for the same reason progressive credit amount is.\nMoney handed over by writing `m_nCurrency` is money the game's bookkeeping\nnever saw, so a starting grant inherits the negative balance on a lost wave and\nthe refund that hands back 400. Item 1's ledger covers a bundle; a starting\ngrant is the same money by a different door, and it wants the same ledger,\nwhich is one more argument for `DistributeCurrencyAmount` over writing the\nproperty.\n\nNote also that this one interacts with the wave-one restart: \"at the start of\nevery Wave 1\" is a mission start, and a mission restarted from the vote menu is\na wave one too. Decide whether restarting pays again before implementing, or\nthe answer will be decided by whichever event handler happened to be wired.","notes":"Imported from TODO.md item 11 — section: Features. All requested, all off by default\nOriginal heading: Feature: Progressive Starting Money. Off by default, open","status":"open","priority":3,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-25T13:53:02Z","created_by":"m-this","updated_at":"2026-08-25T13:53:02Z","labels":["todo-import"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-i3a","title":"Feature: per-class weapon slot unlocks","description":"CaptPurpleHeart: \"I personally would love per-class weapon slot unlocks.\"\n\nToday a slot unlock is one item for every class: `Unlocks_EnforceSlots` strips\na weapon from a slot the run has not opened, without asking who is holding it.\nPer class it is nine items a slot instead of one, which is a much larger pool\nand a much longer run, and both of those are the point of asking for it.\n\nIt is one option, a boolean, off by default: with it off the run keeps today's\none-item-per-slot unlocks and nothing about an existing seed changes.\n\nIt is a gamedata change on the Go side, so it wants an item id per class per\nslot, a regenerated `data.py` and a data format bump, and it should ride the\nsame bump as the Australium Medal in item 7 rather than moving the format\ntwice.\n\nThe open design question is what a run starts with. Nine classes times three\nslots with nothing unlocked is a team that cannot shoot; the honest shape is\nprobably that the primary of the class you spawn as is free and everything else\nis earned, but that is a generation-logic argument and it needs writing down\nbefore any id is minted.","notes":"Imported from TODO.md item 10 — section: Features. All requested, all off by default\nOriginal heading: Feature: per-class weapon slot unlocks. Off by default, open","status":"open","priority":3,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-25T13:53:01Z","created_by":"m-this","updated_at":"2026-08-25T13:53:01Z","labels":["todo-import"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-jzd","title":"Feature: four optional unlocks from Peppy's post","description":"From the second post in\n[`docs/en/discord-mvm-thread.md`](docs/en/discord-mvm-thread.md). Not started,\nand none of them is in `gamedata/items.go` yet.\n\nFour items, one shape: each one is a plugin-side effect on a server-wide\nvalue, and each one gets its own YAML option, off by default, the same shape\nas `death_link` in `apworld/tf2_mvm/options.py`. A seed that leaves them off\ngets none of them in its pool, so an existing seed keeps meaning what it meant.\nThree of the four are progressive, so the option is a count and not a boolean:\nzero copies is off, N copies is the number of steps the run walks.\n\nOrder to build them in is the order below, easiest first. The first two are one\nvalue each and are worth doing before the pool grows further. The last one is\nnot designed yet.\n\n**Progressive Respawn Time.** RED starts on a longer respawn wave time and each\ncopy shortens it back toward the stock value. The candidate is the team respawn\nwave time that MvM already sets per team, not `mp_respawnwavetime` alone;\nconfirm which one MvM actually reads before wiring it. Felt every wave, one\nvalue, no interaction with anything else in the item table.\n\n**Progressive Random Crits.** The team starts with random crits off and each\ncopy raises the chance. `tf_weapon_criticals` and `tf_weapon_criticals_melee`\nare the candidates, but they are booleans, so a chance ramp needs either a\nper-shot hook or a coarse ladder of off, melee only, on. Decide which before\npromising a percentage in the option's docstring. It hits classes unevenly:\nHeavy and Demo notice, Sniper barely does. That is a difficulty knob, not a\nfair one, which is fine for a randomizer as long as the docstring says so.\n\n**Grappling Hook.** One convar, `tf_grapplinghook_enable`, granted once, so a\nstate grant and not a progressive. `useful`, not progression: no access rule\nmay ever need it, because a wave has to stay winnable without it. Cheapest of\nthe four and the largest change to how a map plays.\n\n**Progressive Returning Bomb.** The bomb walks itself back toward the robot\nspawn while no robot carries it, and each copy speeds that walk up. The most\noriginal of the four and the only one that changes the shape of a wave rather\nthan a number. It is also the least specified. Before designing it, find out\nwhat MvM's existing idle-bomb behaviour is and whether a plugin can reach the\nvalue behind it; if the answer is that the bomb only resets on a timer the map\nowns, this becomes a different feature. Note also that it deflates any\nbomb-reset location group, so the two should not ship into one seed without\nsomeone thinking about it.\n\nNot in this batch, and why. Progressive credit amount is blocked on item 1\nabove: money granted by writing `m_nCurrency` does not survive the wave-loss\nrestore, so a credit-scaling unlock inherits the negative balance. Random-effect\ncanteens belong in the trap pool that `spec.md` already describes, not here.\nRandomized weapons with tiers need the tier to be the item id, because ADR 0001\nforbids an id whose meaning is rolled per seed, and that is a feature of its\nown size.","notes":"Imported from TODO.md item 3 — section: Features. All requested, all off by default\nOriginal heading: Feature: four optional unlocks from Peppy's post. Off by default, open","status":"open","priority":3,"issue_type":"feature","owner":"m@this.fr","created_at":"2026-08-25T13:53:01Z","created_by":"m-this","updated_at":"2026-08-25T13:53:01Z","labels":["todo-import"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"apw-gb8","title":"Sign the Windows exe","description":"Players report SmartScreen blocking `tf2ap.exe` and Defender quarantining it.\nNothing is wrong with the binary. The launcher unpacks embedded archives into\nthe TF2 directory, writes Metamod's and SourceMod's DLLs there, downloads a\ngame server and starts it. Behaviourally that is a dropper, and an unsigned\nbinary gives a scanner nothing to weigh against the heuristic. Go makes it\nworse only in that a static Go binary is a shape malware uses a lot; rewriting\nin another language would change nothing.\n\nThe fix is a code signature. The application to the SignPath Foundation, which\nsigns open-source projects for free, is in. `release.yml` already carries the\nsigning step, skipped until `SIGNPATH_API_TOKEN` exists, and it runs before the\nchecksums and the attestation so those describe the signed exe. Granting needs\n`SIGNPATH_API_TOKEN` as a secret and `SIGNPATH_ORGANIZATION_ID` as a variable.\n\nThe application is a long shot and should be treated as one. SignPath asks for\nan established project, and this one was four days old with no stars and about\n130 downloads when it applied. A rejection is the likely answer, and the\nfallback is a paid certificate.\n\nAzure Trusted Signing is the paid fallback at roughly nine euros a month, but\nit wants three years of verifiable identity history from an individual. Do not\nbuy a certificate before SignPath answers.\n\nTwo things are already done and do not need repeating here. The exe carries a\nVERSIONINFO resource and an icon, so it is no longer an anonymous blob to the\nheuristics, and every release publishes SHA-256 sums. Neither removes the\nwarning: only the signature does, plus the download reputation that accrues\nbehind it.\n\nOne thing is worth doing regardless of SignPath. Submit each release to\n[Microsoft's false-positive form](https://www.microsoft.com/en-us/wdsi/filesubmission)\nas a software developer. It is free and they usually clear a file within days.\n\nSigning `tf2ap.exe` does not cover the files it writes. If reports come in\nabout Defender eating the extracted SourceMod DLLs instead of the launcher,\nthe answer is a documented exclusion for the TF2 install path in the Windows\nguide, not more signing.","notes":"Imported from TODO.md item 2 — section: Distribution\nOriginal heading: Sign the Windows exe. Open","status":"deferred","priority":4,"issue_type":"task","owner":"m@this.fr","created_at":"2026-08-25T13:53:00Z","created_by":"m-this","updated_at":"2026-08-26T20:27:07Z","labels":["todo-import"],"comments":[{"id":"01a03cdc-15b2-7237-b89a-8149b5ae86fa","issue_id":"apw-gb8","author":"omae-beads","text":"still waiting on the application on SignPath, no response yet.","created_at":"2026-08-26T06:57:32Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
